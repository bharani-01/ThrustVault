Connecting to PostgreSQL...
SELECT 1 successful: [
  {
    "test": 1
  }
]
Active queries: [
  {
    "pid": 77859,
    "age": {
      "minutes": 11,
      "seconds": 55,
      "milliseconds": 653.724
    },
    "usename": "postgres",
    "state": "active",
    "query": "SELECT max_thrust, recommended_esc, motor_name, custom_parameters FROM motors"
  },
  {
    "pid": 77918,
    "age": {
      "minutes": 10,
      "seconds": 46,
      "milliseconds": 37.909
    },
    "usename": "postgres",
    "state": "active",
    "query": "\n      -- Initialize system settings table\n      CREATE TABLE IF NOT EXISTS public.system_settings (\n          key VARCHAR(255) PRIMARY KEY,\n          value JSONB NOT NULL\n      );\n      INSERT INTO public.system_settings (key, value) \n      VALUES ('auto_approve', 'false'::jsonb) \n      ON CONFLICT (key) DO NOTHING;\n\n      -- Dynamic Migration: Change intern role to user in constraints and tables\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'user_profiles' AND column_name = 'role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'user_profiles' AND column_name = 'role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.user_profiles DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.user_profiles SET role = 'user' WHERE role = 'intern';\n              ALTER TABLE public.user_profiles ADD CONSTRAINT user_profiles_role_check CHECK (role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'access_requests' AND column_name = 'requested_role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'access_requests' AND column_name = 'requested_role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.access_requests DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.access_requests SET requested_role = 'user' WHERE requested_role = 'intern';\n              ALTER TABLE public.access_requests ADD CONSTRAINT access_requests_requested_role_check CHECK (requested_role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      -- Uploader By tracking field migrations\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n      ALTER TABLE public.motor_test_runs ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n\n      -- Add main_image and gallery_images to public.motors\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS main_image TEXT;\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS gallery_images JSONB DEFAULT '[]'::jsonb;\n\n      -- Initialize escs and propellers tables\n      CREATE TABLE IF NOT EXISTS public.escs (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_esc_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE TABLE IF NOT EXISTS public.propellers (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_prop_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE INDEX IF NOT EXIST"
  },
  {
    "pid": 77960,
    "age": {
      "minutes": 9,
      "seconds": 35,
      "milliseconds": 192.205
    },
    "usename": "postgres",
    "state": "active",
    "query": "SELECT category_id, COUNT(*)::int AS cnt FROM motors GROUP BY category_id"
  },
  {
    "pid": 77954,
    "age": {
      "minutes": 9,
      "seconds": 44,
      "milliseconds": 485.465
    },
    "usename": "postgres",
    "state": "active",
    "query": "SELECT id, category_id, motor_name, company, max_thrust,\n                         recommended_esc, recommended_propeller,\n                         link_motor, link_esc, link_propeller, custom_parameters, uploaded_by\n                  FROM motors ORDER BY max_thrust ASC LIMIT $1"
  },
  {
    "pid": 77970,
    "age": {
      "minutes": 9,
      "seconds": 12,
      "milliseconds": 157.384
    },
    "usename": "postgres",
    "state": "active",
    "query": "SELECT id, role FROM user_profiles WHERE email = $1"
  },
  {
    "pid": 77964,
    "age": {
      "minutes": 9,
      "seconds": 42,
      "milliseconds": 852.682
    },
    "usename": "postgres",
    "state": "active",
    "query": "SELECT DISTINCT company FROM motors WHERE company IS NOT NULL AND company != '' ORDER BY company"
  },
  {
    "pid": 77959,
    "age": {
      "minutes": 9,
      "seconds": 44,
      "milliseconds": 484.907
    },
    "usename": "postgres",
    "state": "active",
    "query": "SELECT category_id, COUNT(*)::int AS cnt FROM motors GROUP BY category_id"
  },
  {
    "pid": 77961,
    "age": {
      "minutes": 9,
      "seconds": 35,
      "milliseconds": 180.265
    },
    "usename": "postgres",
    "state": "active",
    "query": "SELECT id, category_id, motor_name, company, max_thrust,\n                         recommended_esc, recommended_propeller,\n                         link_motor, link_esc, link_propeller, custom_parameters, uploaded_by\n                  FROM motors ORDER BY max_thrust ASC LIMIT $1"
  },
  {
    "pid": 77958,
    "age": {
      "minutes": 9,
      "seconds": 35,
      "milliseconds": 147.169
    },
    "usename": "postgres",
    "state": "active",
    "query": "SELECT DISTINCT company FROM motors WHERE company IS NOT NULL AND company != '' ORDER BY company"
  },
  {
    "pid": 78164,
    "age": {
      "minutes": 4,
      "seconds": 26,
      "milliseconds": 380.991
    },
    "usename": "postgres",
    "state": "active",
    "query": "\n      -- Initialize system settings table\n      CREATE TABLE IF NOT EXISTS public.system_settings (\n          key VARCHAR(255) PRIMARY KEY,\n          value JSONB NOT NULL\n      );\n      INSERT INTO public.system_settings (key, value) \n      VALUES ('auto_approve', 'false'::jsonb) \n      ON CONFLICT (key) DO NOTHING;\n\n      -- Dynamic Migration: Change intern role to user in constraints and tables\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'user_profiles' AND column_name = 'role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'user_profiles' AND column_name = 'role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.user_profiles DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.user_profiles SET role = 'user' WHERE role = 'intern';\n              ALTER TABLE public.user_profiles ADD CONSTRAINT user_profiles_role_check CHECK (role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'access_requests' AND column_name = 'requested_role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'access_requests' AND column_name = 'requested_role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.access_requests DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.access_requests SET requested_role = 'user' WHERE requested_role = 'intern';\n              ALTER TABLE public.access_requests ADD CONSTRAINT access_requests_requested_role_check CHECK (requested_role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      -- Uploader By tracking field migrations\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n      ALTER TABLE public.motor_test_runs ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n\n      -- Add main_image and gallery_images to public.motors\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS main_image TEXT;\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS gallery_images JSONB DEFAULT '[]'::jsonb;\n\n      -- Initialize escs and propellers tables\n      CREATE TABLE IF NOT EXISTS public.escs (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_esc_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE TABLE IF NOT EXISTS public.propellers (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_prop_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE INDEX IF NOT EXIST"
  },
  {
    "pid": 78291,
    "age": {
      "minutes": 1,
      "seconds": 1,
      "milliseconds": 334.579
    },
    "usename": "postgres",
    "state": "active",
    "query": "\n      -- Initialize system settings table\n      CREATE TABLE IF NOT EXISTS public.system_settings (\n          key VARCHAR(255) PRIMARY KEY,\n          value JSONB NOT NULL\n      );\n      INSERT INTO public.system_settings (key, value) \n      VALUES ('auto_approve', 'false'::jsonb) \n      ON CONFLICT (key) DO NOTHING;\n\n      -- Dynamic Migration: Change intern role to user in constraints and tables\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'user_profiles' AND column_name = 'role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'user_profiles' AND column_name = 'role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.user_profiles DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.user_profiles SET role = 'user' WHERE role = 'intern';\n              ALTER TABLE public.user_profiles ADD CONSTRAINT user_profiles_role_check CHECK (role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'access_requests' AND column_name = 'requested_role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'access_requests' AND column_name = 'requested_role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.access_requests DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.access_requests SET requested_role = 'user' WHERE requested_role = 'intern';\n              ALTER TABLE public.access_requests ADD CONSTRAINT access_requests_requested_role_check CHECK (requested_role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      -- Uploader By tracking field migrations\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n      ALTER TABLE public.motor_test_runs ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n\n      -- Add main_image and gallery_images to public.motors\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS main_image TEXT;\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS gallery_images JSONB DEFAULT '[]'::jsonb;\n\n      -- Initialize escs and propellers tables\n      CREATE TABLE IF NOT EXISTS public.escs (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_esc_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE TABLE IF NOT EXISTS public.propellers (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_prop_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE INDEX IF NOT EXIST"
  },
  {
    "pid": 78134,
    "age": {
      "minutes": 4,
      "seconds": 59,
      "milliseconds": 162.676
    },
    "usename": "postgres",
    "state": "active",
    "query": "SELECT id, role FROM user_profiles WHERE email = $1"
  },
  {
    "pid": 78300,
    "age": {
      "seconds": 48,
      "milliseconds": 938.285
    },
    "usename": "postgres",
    "state": "active",
    "query": "SELECT * FROM public.user_profiles ORDER BY email"
  }
]
Blocked/Blocking locks: [
  {
    "locked_item": "user_profiles",
    "blocked_pid": 77970,
    "blocked_query": "SELECT id, role FROM user_profiles WHERE email = $1",
    "blocked_mode": "AccessShareLock",
    "blocking_pid": 77918,
    "blocking_query": "\n      -- Initialize system settings table\n      CREATE TABLE IF NOT EXISTS public.system_settings (\n          key VARCHAR(255) PRIMARY KEY,\n          value JSONB NOT NULL\n      );\n      INSERT INTO public.system_settings (key, value) \n      VALUES ('auto_approve', 'false'::jsonb) \n      ON CONFLICT (key) DO NOTHING;\n\n      -- Dynamic Migration: Change intern role to user in constraints and tables\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'user_profiles' AND column_name = 'role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'user_profiles' AND column_name = 'role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.user_profiles DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.user_profiles SET role = 'user' WHERE role = 'intern';\n              ALTER TABLE public.user_profiles ADD CONSTRAINT user_profiles_role_check CHECK (role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'access_requests' AND column_name = 'requested_role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'access_requests' AND column_name = 'requested_role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.access_requests DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.access_requests SET requested_role = 'user' WHERE requested_role = 'intern';\n              ALTER TABLE public.access_requests ADD CONSTRAINT access_requests_requested_role_check CHECK (requested_role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      -- Uploader By tracking field migrations\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n      ALTER TABLE public.motor_test_runs ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n\n      -- Add main_image and gallery_images to public.motors\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS main_image TEXT;\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS gallery_images JSONB DEFAULT '[]'::jsonb;\n\n      -- Initialize escs and propellers tables\n      CREATE TABLE IF NOT EXISTS public.escs (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_esc_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE TABLE IF NOT EXISTS public.propellers (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_prop_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE INDEX IF NOT EXIST",
    "blocking_mode": "AccessExclusiveLock"
  },
  {
    "locked_item": "user_profiles",
    "blocked_pid": 77970,
    "blocked_query": "SELECT id, role FROM user_profiles WHERE email = $1",
    "blocked_mode": "AccessShareLock",
    "blocking_pid": 77918,
    "blocking_query": "\n      -- Initialize system settings table\n      CREATE TABLE IF NOT EXISTS public.system_settings (\n          key VARCHAR(255) PRIMARY KEY,\n          value JSONB NOT NULL\n      );\n      INSERT INTO public.system_settings (key, value) \n      VALUES ('auto_approve', 'false'::jsonb) \n      ON CONFLICT (key) DO NOTHING;\n\n      -- Dynamic Migration: Change intern role to user in constraints and tables\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'user_profiles' AND column_name = 'role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'user_profiles' AND column_name = 'role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.user_profiles DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.user_profiles SET role = 'user' WHERE role = 'intern';\n              ALTER TABLE public.user_profiles ADD CONSTRAINT user_profiles_role_check CHECK (role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'access_requests' AND column_name = 'requested_role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'access_requests' AND column_name = 'requested_role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.access_requests DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.access_requests SET requested_role = 'user' WHERE requested_role = 'intern';\n              ALTER TABLE public.access_requests ADD CONSTRAINT access_requests_requested_role_check CHECK (requested_role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      -- Uploader By tracking field migrations\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n      ALTER TABLE public.motor_test_runs ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n\n      -- Add main_image and gallery_images to public.motors\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS main_image TEXT;\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS gallery_images JSONB DEFAULT '[]'::jsonb;\n\n      -- Initialize escs and propellers tables\n      CREATE TABLE IF NOT EXISTS public.escs (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_esc_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE TABLE IF NOT EXISTS public.propellers (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_prop_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE INDEX IF NOT EXIST",
    "blocking_mode": "RowExclusiveLock"
  },
  {
    "locked_item": "user_profiles",
    "blocked_pid": 77970,
    "blocked_query": "SELECT id, role FROM user_profiles WHERE email = $1",
    "blocked_mode": "AccessShareLock",
    "blocking_pid": 78164,
    "blocking_query": "\n      -- Initialize system settings table\n      CREATE TABLE IF NOT EXISTS public.system_settings (\n          key VARCHAR(255) PRIMARY KEY,\n          value JSONB NOT NULL\n      );\n      INSERT INTO public.system_settings (key, value) \n      VALUES ('auto_approve', 'false'::jsonb) \n      ON CONFLICT (key) DO NOTHING;\n\n      -- Dynamic Migration: Change intern role to user in constraints and tables\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'user_profiles' AND column_name = 'role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'user_profiles' AND column_name = 'role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.user_profiles DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.user_profiles SET role = 'user' WHERE role = 'intern';\n              ALTER TABLE public.user_profiles ADD CONSTRAINT user_profiles_role_check CHECK (role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'access_requests' AND column_name = 'requested_role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'access_requests' AND column_name = 'requested_role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.access_requests DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.access_requests SET requested_role = 'user' WHERE requested_role = 'intern';\n              ALTER TABLE public.access_requests ADD CONSTRAINT access_requests_requested_role_check CHECK (requested_role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      -- Uploader By tracking field migrations\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n      ALTER TABLE public.motor_test_runs ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n\n      -- Add main_image and gallery_images to public.motors\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS main_image TEXT;\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS gallery_images JSONB DEFAULT '[]'::jsonb;\n\n      -- Initialize escs and propellers tables\n      CREATE TABLE IF NOT EXISTS public.escs (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_esc_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE TABLE IF NOT EXISTS public.propellers (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_prop_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE INDEX IF NOT EXIST",
    "blocking_mode": "AccessExclusiveLock"
  },
  {
    "locked_item": "user_profiles",
    "blocked_pid": 77970,
    "blocked_query": "SELECT id, role FROM user_profiles WHERE email = $1",
    "blocked_mode": "AccessShareLock",
    "blocking_pid": 78291,
    "blocking_query": "\n      -- Initialize system settings table\n      CREATE TABLE IF NOT EXISTS public.system_settings (\n          key VARCHAR(255) PRIMARY KEY,\n          value JSONB NOT NULL\n      );\n      INSERT INTO public.system_settings (key, value) \n      VALUES ('auto_approve', 'false'::jsonb) \n      ON CONFLICT (key) DO NOTHING;\n\n      -- Dynamic Migration: Change intern role to user in constraints and tables\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'user_profiles' AND column_name = 'role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'user_profiles' AND column_name = 'role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.user_profiles DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.user_profiles SET role = 'user' WHERE role = 'intern';\n              ALTER TABLE public.user_profiles ADD CONSTRAINT user_profiles_role_check CHECK (role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'access_requests' AND column_name = 'requested_role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'access_requests' AND column_name = 'requested_role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.access_requests DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.access_requests SET requested_role = 'user' WHERE requested_role = 'intern';\n              ALTER TABLE public.access_requests ADD CONSTRAINT access_requests_requested_role_check CHECK (requested_role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      -- Uploader By tracking field migrations\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n      ALTER TABLE public.motor_test_runs ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n\n      -- Add main_image and gallery_images to public.motors\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS main_image TEXT;\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS gallery_images JSONB DEFAULT '[]'::jsonb;\n\n      -- Initialize escs and propellers tables\n      CREATE TABLE IF NOT EXISTS public.escs (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_esc_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE TABLE IF NOT EXISTS public.propellers (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_prop_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE INDEX IF NOT EXIST",
    "blocking_mode": "AccessExclusiveLock"
  },
  {
    "locked_item": "user_profiles",
    "blocked_pid": 77970,
    "blocked_query": "SELECT id, role FROM user_profiles WHERE email = $1",
    "blocked_mode": "AccessShareLock",
    "blocking_pid": 78134,
    "blocking_query": "SELECT id, role FROM user_profiles WHERE email = $1",
    "blocking_mode": "AccessShareLock"
  },
  {
    "locked_item": "user_profiles",
    "blocked_pid": 77970,
    "blocked_query": "SELECT id, role FROM user_profiles WHERE email = $1",
    "blocked_mode": "AccessShareLock",
    "blocking_pid": 78300,
    "blocking_query": "SELECT * FROM public.user_profiles ORDER BY email",
    "blocking_mode": "AccessShareLock"
  },
  {
    "locked_item": "user_profiles",
    "blocked_pid": 78134,
    "blocked_query": "SELECT id, role FROM user_profiles WHERE email = $1",
    "blocked_mode": "AccessShareLock",
    "blocking_pid": 77918,
    "blocking_query": "\n      -- Initialize system settings table\n      CREATE TABLE IF NOT EXISTS public.system_settings (\n          key VARCHAR(255) PRIMARY KEY,\n          value JSONB NOT NULL\n      );\n      INSERT INTO public.system_settings (key, value) \n      VALUES ('auto_approve', 'false'::jsonb) \n      ON CONFLICT (key) DO NOTHING;\n\n      -- Dynamic Migration: Change intern role to user in constraints and tables\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'user_profiles' AND column_name = 'role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'user_profiles' AND column_name = 'role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.user_profiles DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.user_profiles SET role = 'user' WHERE role = 'intern';\n              ALTER TABLE public.user_profiles ADD CONSTRAINT user_profiles_role_check CHECK (role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'access_requests' AND column_name = 'requested_role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'access_requests' AND column_name = 'requested_role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.access_requests DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.access_requests SET requested_role = 'user' WHERE requested_role = 'intern';\n              ALTER TABLE public.access_requests ADD CONSTRAINT access_requests_requested_role_check CHECK (requested_role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      -- Uploader By tracking field migrations\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n      ALTER TABLE public.motor_test_runs ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n\n      -- Add main_image and gallery_images to public.motors\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS main_image TEXT;\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS gallery_images JSONB DEFAULT '[]'::jsonb;\n\n      -- Initialize escs and propellers tables\n      CREATE TABLE IF NOT EXISTS public.escs (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_esc_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE TABLE IF NOT EXISTS public.propellers (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_prop_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE INDEX IF NOT EXIST",
    "blocking_mode": "AccessExclusiveLock"
  },
  {
    "locked_item": "user_profiles",
    "blocked_pid": 78134,
    "blocked_query": "SELECT id, role FROM user_profiles WHERE email = $1",
    "blocked_mode": "AccessShareLock",
    "blocking_pid": 77918,
    "blocking_query": "\n      -- Initialize system settings table\n      CREATE TABLE IF NOT EXISTS public.system_settings (\n          key VARCHAR(255) PRIMARY KEY,\n          value JSONB NOT NULL\n      );\n      INSERT INTO public.system_settings (key, value) \n      VALUES ('auto_approve', 'false'::jsonb) \n      ON CONFLICT (key) DO NOTHING;\n\n      -- Dynamic Migration: Change intern role to user in constraints and tables\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'user_profiles' AND column_name = 'role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'user_profiles' AND column_name = 'role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.user_profiles DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.user_profiles SET role = 'user' WHERE role = 'intern';\n              ALTER TABLE public.user_profiles ADD CONSTRAINT user_profiles_role_check CHECK (role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'access_requests' AND column_name = 'requested_role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'access_requests' AND column_name = 'requested_role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.access_requests DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.access_requests SET requested_role = 'user' WHERE requested_role = 'intern';\n              ALTER TABLE public.access_requests ADD CONSTRAINT access_requests_requested_role_check CHECK (requested_role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      -- Uploader By tracking field migrations\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n      ALTER TABLE public.motor_test_runs ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n\n      -- Add main_image and gallery_images to public.motors\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS main_image TEXT;\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS gallery_images JSONB DEFAULT '[]'::jsonb;\n\n      -- Initialize escs and propellers tables\n      CREATE TABLE IF NOT EXISTS public.escs (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_esc_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE TABLE IF NOT EXISTS public.propellers (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_prop_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE INDEX IF NOT EXIST",
    "blocking_mode": "RowExclusiveLock"
  },
  {
    "locked_item": "user_profiles",
    "blocked_pid": 78134,
    "blocked_query": "SELECT id, role FROM user_profiles WHERE email = $1",
    "blocked_mode": "AccessShareLock",
    "blocking_pid": 77970,
    "blocking_query": "SELECT id, role FROM user_profiles WHERE email = $1",
    "blocking_mode": "AccessShareLock"
  },
  {
    "locked_item": "user_profiles",
    "blocked_pid": 78134,
    "blocked_query": "SELECT id, role FROM user_profiles WHERE email = $1",
    "blocked_mode": "AccessShareLock",
    "blocking_pid": 78164,
    "blocking_query": "\n      -- Initialize system settings table\n      CREATE TABLE IF NOT EXISTS public.system_settings (\n          key VARCHAR(255) PRIMARY KEY,\n          value JSONB NOT NULL\n      );\n      INSERT INTO public.system_settings (key, value) \n      VALUES ('auto_approve', 'false'::jsonb) \n      ON CONFLICT (key) DO NOTHING;\n\n      -- Dynamic Migration: Change intern role to user in constraints and tables\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'user_profiles' AND column_name = 'role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'user_profiles' AND column_name = 'role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.user_profiles DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.user_profiles SET role = 'user' WHERE role = 'intern';\n              ALTER TABLE public.user_profiles ADD CONSTRAINT user_profiles_role_check CHECK (role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'access_requests' AND column_name = 'requested_role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'access_requests' AND column_name = 'requested_role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.access_requests DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.access_requests SET requested_role = 'user' WHERE requested_role = 'intern';\n              ALTER TABLE public.access_requests ADD CONSTRAINT access_requests_requested_role_check CHECK (requested_role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      -- Uploader By tracking field migrations\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n      ALTER TABLE public.motor_test_runs ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n\n      -- Add main_image and gallery_images to public.motors\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS main_image TEXT;\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS gallery_images JSONB DEFAULT '[]'::jsonb;\n\n      -- Initialize escs and propellers tables\n      CREATE TABLE IF NOT EXISTS public.escs (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_esc_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE TABLE IF NOT EXISTS public.propellers (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_prop_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE INDEX IF NOT EXIST",
    "blocking_mode": "AccessExclusiveLock"
  },
  {
    "locked_item": "user_profiles",
    "blocked_pid": 78134,
    "blocked_query": "SELECT id, role FROM user_profiles WHERE email = $1",
    "blocked_mode": "AccessShareLock",
    "blocking_pid": 78291,
    "blocking_query": "\n      -- Initialize system settings table\n      CREATE TABLE IF NOT EXISTS public.system_settings (\n          key VARCHAR(255) PRIMARY KEY,\n          value JSONB NOT NULL\n      );\n      INSERT INTO public.system_settings (key, value) \n      VALUES ('auto_approve', 'false'::jsonb) \n      ON CONFLICT (key) DO NOTHING;\n\n      -- Dynamic Migration: Change intern role to user in constraints and tables\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'user_profiles' AND column_name = 'role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'user_profiles' AND column_name = 'role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.user_profiles DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.user_profiles SET role = 'user' WHERE role = 'intern';\n              ALTER TABLE public.user_profiles ADD CONSTRAINT user_profiles_role_check CHECK (role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'access_requests' AND column_name = 'requested_role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'access_requests' AND column_name = 'requested_role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.access_requests DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.access_requests SET requested_role = 'user' WHERE requested_role = 'intern';\n              ALTER TABLE public.access_requests ADD CONSTRAINT access_requests_requested_role_check CHECK (requested_role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      -- Uploader By tracking field migrations\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n      ALTER TABLE public.motor_test_runs ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n\n      -- Add main_image and gallery_images to public.motors\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS main_image TEXT;\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS gallery_images JSONB DEFAULT '[]'::jsonb;\n\n      -- Initialize escs and propellers tables\n      CREATE TABLE IF NOT EXISTS public.escs (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_esc_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE TABLE IF NOT EXISTS public.propellers (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_prop_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE INDEX IF NOT EXIST",
    "blocking_mode": "AccessExclusiveLock"
  },
  {
    "locked_item": "user_profiles",
    "blocked_pid": 78134,
    "blocked_query": "SELECT id, role FROM user_profiles WHERE email = $1",
    "blocked_mode": "AccessShareLock",
    "blocking_pid": 78300,
    "blocking_query": "SELECT * FROM public.user_profiles ORDER BY email",
    "blocking_mode": "AccessShareLock"
  },
  {
    "locked_item": "motors",
    "blocked_pid": 77964,
    "blocked_query": "SELECT DISTINCT company FROM motors WHERE company IS NOT NULL AND company != '' ORDER BY company",
    "blocked_mode": "AccessShareLock",
    "blocking_pid": 77859,
    "blocking_query": "SELECT max_thrust, recommended_esc, motor_name, custom_parameters FROM motors",
    "blocking_mode": "AccessShareLock"
  },
  {
    "locked_item": "motors",
    "blocked_pid": 77964,
    "blocked_query": "SELECT DISTINCT company FROM motors WHERE company IS NOT NULL AND company != '' ORDER BY company",
    "blocked_mode": "AccessShareLock",
    "blocking_pid": 77918,
    "blocking_query": "\n      -- Initialize system settings table\n      CREATE TABLE IF NOT EXISTS public.system_settings (\n          key VARCHAR(255) PRIMARY KEY,\n          value JSONB NOT NULL\n      );\n      INSERT INTO public.system_settings (key, value) \n      VALUES ('auto_approve', 'false'::jsonb) \n      ON CONFLICT (key) DO NOTHING;\n\n      -- Dynamic Migration: Change intern role to user in constraints and tables\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'user_profiles' AND column_name = 'role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'user_profiles' AND column_name = 'role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.user_profiles DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.user_profiles SET role = 'user' WHERE role = 'intern';\n              ALTER TABLE public.user_profiles ADD CONSTRAINT user_profiles_role_check CHECK (role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'access_requests' AND column_name = 'requested_role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'access_requests' AND column_name = 'requested_role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.access_requests DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.access_requests SET requested_role = 'user' WHERE requested_role = 'intern';\n              ALTER TABLE public.access_requests ADD CONSTRAINT access_requests_requested_role_check CHECK (requested_role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      -- Uploader By tracking field migrations\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n      ALTER TABLE public.motor_test_runs ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n\n      -- Add main_image and gallery_images to public.motors\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS main_image TEXT;\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS gallery_images JSONB DEFAULT '[]'::jsonb;\n\n      -- Initialize escs and propellers tables\n      CREATE TABLE IF NOT EXISTS public.escs (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_esc_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE TABLE IF NOT EXISTS public.propellers (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_prop_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE INDEX IF NOT EXIST",
    "blocking_mode": "AccessExclusiveLock"
  },
  {
    "locked_item": "motors",
    "blocked_pid": 77964,
    "blocked_query": "SELECT DISTINCT company FROM motors WHERE company IS NOT NULL AND company != '' ORDER BY company",
    "blocked_mode": "AccessShareLock",
    "blocking_pid": 77960,
    "blocking_query": "SELECT category_id, COUNT(*)::int AS cnt FROM motors GROUP BY category_id",
    "blocking_mode": "AccessShareLock"
  },
  {
    "locked_item": "motors",
    "blocked_pid": 77964,
    "blocked_query": "SELECT DISTINCT company FROM motors WHERE company IS NOT NULL AND company != '' ORDER BY company",
    "blocked_mode": "AccessShareLock",
    "blocking_pid": 77954,
    "blocking_query": "SELECT id, category_id, motor_name, company, max_thrust,\n                         recommended_esc, recommended_propeller,\n                         link_motor, link_esc, link_propeller, custom_parameters, uploaded_by\n                  FROM motors ORDER BY max_thrust ASC LIMIT $1",
    "blocking_mode": "AccessShareLock"
  },
  {
    "locked_item": "motors",
    "blocked_pid": 77964,
    "blocked_query": "SELECT DISTINCT company FROM motors WHERE company IS NOT NULL AND company != '' ORDER BY company",
    "blocked_mode": "AccessShareLock",
    "blocking_pid": 77959,
    "blocking_query": "SELECT category_id, COUNT(*)::int AS cnt FROM motors GROUP BY category_id",
    "blocking_mode": "AccessShareLock"
  },
  {
    "locked_item": "motors",
    "blocked_pid": 77964,
    "blocked_query": "SELECT DISTINCT company FROM motors WHERE company IS NOT NULL AND company != '' ORDER BY company",
    "blocked_mode": "AccessShareLock",
    "blocking_pid": 77961,
    "blocking_query": "SELECT id, category_id, motor_name, company, max_thrust,\n                         recommended_esc, recommended_propeller,\n                         link_motor, link_esc, link_propeller, custom_parameters, uploaded_by\n                  FROM motors ORDER BY max_thrust ASC LIMIT $1",
    "blocking_mode": "AccessShareLock"
  },
  {
    "locked_item": "motors",
    "blocked_pid": 77964,
    "blocked_query": "SELECT DISTINCT company FROM motors WHERE company IS NOT NULL AND company != '' ORDER BY company",
    "blocked_mode": "AccessShareLock",
    "blocking_pid": 77958,
    "blocking_query": "SELECT DISTINCT company FROM motors WHERE company IS NOT NULL AND company != '' ORDER BY company",
    "blocking_mode": "AccessShareLock"
  },
  {
    "locked_item": "user_profiles",
    "blocked_pid": 78291,
    "blocked_query": "\n      -- Initialize system settings table\n      CREATE TABLE IF NOT EXISTS public.system_settings (\n          key VARCHAR(255) PRIMARY KEY,\n          value JSONB NOT NULL\n      );\n      INSERT INTO public.system_settings (key, value) \n      VALUES ('auto_approve', 'false'::jsonb) \n      ON CONFLICT (key) DO NOTHING;\n\n      -- Dynamic Migration: Change intern role to user in constraints and tables\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'user_profiles' AND column_name = 'role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'user_profiles' AND column_name = 'role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.user_profiles DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.user_profiles SET role = 'user' WHERE role = 'intern';\n              ALTER TABLE public.user_profiles ADD CONSTRAINT user_profiles_role_check CHECK (role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'access_requests' AND column_name = 'requested_role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'access_requests' AND column_name = 'requested_role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.access_requests DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.access_requests SET requested_role = 'user' WHERE requested_role = 'intern';\n              ALTER TABLE public.access_requests ADD CONSTRAINT access_requests_requested_role_check CHECK (requested_role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      -- Uploader By tracking field migrations\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n      ALTER TABLE public.motor_test_runs ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n\n      -- Add main_image and gallery_images to public.motors\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS main_image TEXT;\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS gallery_images JSONB DEFAULT '[]'::jsonb;\n\n      -- Initialize escs and propellers tables\n      CREATE TABLE IF NOT EXISTS public.escs (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_esc_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE TABLE IF NOT EXISTS public.propellers (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_prop_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE INDEX IF NOT EXIST",
    "blocked_mode": "AccessExclusiveLock",
    "blocking_pid": 77918,
    "blocking_query": "\n      -- Initialize system settings table\n      CREATE TABLE IF NOT EXISTS public.system_settings (\n          key VARCHAR(255) PRIMARY KEY,\n          value JSONB NOT NULL\n      );\n      INSERT INTO public.system_settings (key, value) \n      VALUES ('auto_approve', 'false'::jsonb) \n      ON CONFLICT (key) DO NOTHING;\n\n      -- Dynamic Migration: Change intern role to user in constraints and tables\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'user_profiles' AND column_name = 'role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'user_profiles' AND column_name = 'role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.user_profiles DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.user_profiles SET role = 'user' WHERE role = 'intern';\n              ALTER TABLE public.user_profiles ADD CONSTRAINT user_profiles_role_check CHECK (role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'access_requests' AND column_name = 'requested_role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'access_requests' AND column_name = 'requested_role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.access_requests DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.access_requests SET requested_role = 'user' WHERE requested_role = 'intern';\n              ALTER TABLE public.access_requests ADD CONSTRAINT access_requests_requested_role_check CHECK (requested_role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      -- Uploader By tracking field migrations\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n      ALTER TABLE public.motor_test_runs ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n\n      -- Add main_image and gallery_images to public.motors\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS main_image TEXT;\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS gallery_images JSONB DEFAULT '[]'::jsonb;\n\n      -- Initialize escs and propellers tables\n      CREATE TABLE IF NOT EXISTS public.escs (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_esc_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE TABLE IF NOT EXISTS public.propellers (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_prop_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE INDEX IF NOT EXIST",
    "blocking_mode": "AccessExclusiveLock"
  },
  {
    "locked_item": "user_profiles",
    "blocked_pid": 78291,
    "blocked_query": "\n      -- Initialize system settings table\n      CREATE TABLE IF NOT EXISTS public.system_settings (\n          key VARCHAR(255) PRIMARY KEY,\n          value JSONB NOT NULL\n      );\n      INSERT INTO public.system_settings (key, value) \n      VALUES ('auto_approve', 'false'::jsonb) \n      ON CONFLICT (key) DO NOTHING;\n\n      -- Dynamic Migration: Change intern role to user in constraints and tables\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'user_profiles' AND column_name = 'role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'user_profiles' AND column_name = 'role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.user_profiles DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.user_profiles SET role = 'user' WHERE role = 'intern';\n              ALTER TABLE public.user_profiles ADD CONSTRAINT user_profiles_role_check CHECK (role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'access_requests' AND column_name = 'requested_role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'access_requests' AND column_name = 'requested_role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.access_requests DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.access_requests SET requested_role = 'user' WHERE requested_role = 'intern';\n              ALTER TABLE public.access_requests ADD CONSTRAINT access_requests_requested_role_check CHECK (requested_role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      -- Uploader By tracking field migrations\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n      ALTER TABLE public.motor_test_runs ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n\n      -- Add main_image and gallery_images to public.motors\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS main_image TEXT;\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS gallery_images JSONB DEFAULT '[]'::jsonb;\n\n      -- Initialize escs and propellers tables\n      CREATE TABLE IF NOT EXISTS public.escs (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_esc_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE TABLE IF NOT EXISTS public.propellers (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_prop_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE INDEX IF NOT EXIST",
    "blocked_mode": "AccessExclusiveLock",
    "blocking_pid": 77918,
    "blocking_query": "\n      -- Initialize system settings table\n      CREATE TABLE IF NOT EXISTS public.system_settings (\n          key VARCHAR(255) PRIMARY KEY,\n          value JSONB NOT NULL\n      );\n      INSERT INTO public.system_settings (key, value) \n      VALUES ('auto_approve', 'false'::jsonb) \n      ON CONFLICT (key) DO NOTHING;\n\n      -- Dynamic Migration: Change intern role to user in constraints and tables\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'user_profiles' AND column_name = 'role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'user_profiles' AND column_name = 'role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.user_profiles DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.user_profiles SET role = 'user' WHERE role = 'intern';\n              ALTER TABLE public.user_profiles ADD CONSTRAINT user_profiles_role_check CHECK (role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'access_requests' AND column_name = 'requested_role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'access_requests' AND column_name = 'requested_role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.access_requests DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.access_requests SET requested_role = 'user' WHERE requested_role = 'intern';\n              ALTER TABLE public.access_requests ADD CONSTRAINT access_requests_requested_role_check CHECK (requested_role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      -- Uploader By tracking field migrations\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n      ALTER TABLE public.motor_test_runs ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n\n      -- Add main_image and gallery_images to public.motors\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS main_image TEXT;\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS gallery_images JSONB DEFAULT '[]'::jsonb;\n\n      -- Initialize escs and propellers tables\n      CREATE TABLE IF NOT EXISTS public.escs (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_esc_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE TABLE IF NOT EXISTS public.propellers (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_prop_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE INDEX IF NOT EXIST",
    "blocking_mode": "RowExclusiveLock"
  },
  {
    "locked_item": "user_profiles",
    "blocked_pid": 78291,
    "blocked_query": "\n      -- Initialize system settings table\n      CREATE TABLE IF NOT EXISTS public.system_settings (\n          key VARCHAR(255) PRIMARY KEY,\n          value JSONB NOT NULL\n      );\n      INSERT INTO public.system_settings (key, value) \n      VALUES ('auto_approve', 'false'::jsonb) \n      ON CONFLICT (key) DO NOTHING;\n\n      -- Dynamic Migration: Change intern role to user in constraints and tables\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'user_profiles' AND column_name = 'role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'user_profiles' AND column_name = 'role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.user_profiles DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.user_profiles SET role = 'user' WHERE role = 'intern';\n              ALTER TABLE public.user_profiles ADD CONSTRAINT user_profiles_role_check CHECK (role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'access_requests' AND column_name = 'requested_role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'access_requests' AND column_name = 'requested_role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.access_requests DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.access_requests SET requested_role = 'user' WHERE requested_role = 'intern';\n              ALTER TABLE public.access_requests ADD CONSTRAINT access_requests_requested_role_check CHECK (requested_role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      -- Uploader By tracking field migrations\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n      ALTER TABLE public.motor_test_runs ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n\n      -- Add main_image and gallery_images to public.motors\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS main_image TEXT;\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS gallery_images JSONB DEFAULT '[]'::jsonb;\n\n      -- Initialize escs and propellers tables\n      CREATE TABLE IF NOT EXISTS public.escs (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_esc_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE TABLE IF NOT EXISTS public.propellers (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_prop_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE INDEX IF NOT EXIST",
    "blocked_mode": "AccessExclusiveLock",
    "blocking_pid": 77970,
    "blocking_query": "SELECT id, role FROM user_profiles WHERE email = $1",
    "blocking_mode": "AccessShareLock"
  },
  {
    "locked_item": "user_profiles",
    "blocked_pid": 78291,
    "blocked_query": "\n      -- Initialize system settings table\n      CREATE TABLE IF NOT EXISTS public.system_settings (\n          key VARCHAR(255) PRIMARY KEY,\n          value JSONB NOT NULL\n      );\n      INSERT INTO public.system_settings (key, value) \n      VALUES ('auto_approve', 'false'::jsonb) \n      ON CONFLICT (key) DO NOTHING;\n\n      -- Dynamic Migration: Change intern role to user in constraints and tables\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'user_profiles' AND column_name = 'role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'user_profiles' AND column_name = 'role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.user_profiles DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.user_profiles SET role = 'user' WHERE role = 'intern';\n              ALTER TABLE public.user_profiles ADD CONSTRAINT user_profiles_role_check CHECK (role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'access_requests' AND column_name = 'requested_role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'access_requests' AND column_name = 'requested_role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.access_requests DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.access_requests SET requested_role = 'user' WHERE requested_role = 'intern';\n              ALTER TABLE public.access_requests ADD CONSTRAINT access_requests_requested_role_check CHECK (requested_role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      -- Uploader By tracking field migrations\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n      ALTER TABLE public.motor_test_runs ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n\n      -- Add main_image and gallery_images to public.motors\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS main_image TEXT;\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS gallery_images JSONB DEFAULT '[]'::jsonb;\n\n      -- Initialize escs and propellers tables\n      CREATE TABLE IF NOT EXISTS public.escs (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_esc_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE TABLE IF NOT EXISTS public.propellers (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_prop_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE INDEX IF NOT EXIST",
    "blocked_mode": "AccessExclusiveLock",
    "blocking_pid": 78164,
    "blocking_query": "\n      -- Initialize system settings table\n      CREATE TABLE IF NOT EXISTS public.system_settings (\n          key VARCHAR(255) PRIMARY KEY,\n          value JSONB NOT NULL\n      );\n      INSERT INTO public.system_settings (key, value) \n      VALUES ('auto_approve', 'false'::jsonb) \n      ON CONFLICT (key) DO NOTHING;\n\n      -- Dynamic Migration: Change intern role to user in constraints and tables\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'user_profiles' AND column_name = 'role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'user_profiles' AND column_name = 'role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.user_profiles DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.user_profiles SET role = 'user' WHERE role = 'intern';\n              ALTER TABLE public.user_profiles ADD CONSTRAINT user_profiles_role_check CHECK (role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'access_requests' AND column_name = 'requested_role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'access_requests' AND column_name = 'requested_role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.access_requests DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.access_requests SET requested_role = 'user' WHERE requested_role = 'intern';\n              ALTER TABLE public.access_requests ADD CONSTRAINT access_requests_requested_role_check CHECK (requested_role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      -- Uploader By tracking field migrations\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n      ALTER TABLE public.motor_test_runs ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n\n      -- Add main_image and gallery_images to public.motors\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS main_image TEXT;\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS gallery_images JSONB DEFAULT '[]'::jsonb;\n\n      -- Initialize escs and propellers tables\n      CREATE TABLE IF NOT EXISTS public.escs (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_esc_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE TABLE IF NOT EXISTS public.propellers (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_prop_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE INDEX IF NOT EXIST",
    "blocking_mode": "AccessExclusiveLock"
  },
  {
    "locked_item": "user_profiles",
    "blocked_pid": 78291,
    "blocked_query": "\n      -- Initialize system settings table\n      CREATE TABLE IF NOT EXISTS public.system_settings (\n          key VARCHAR(255) PRIMARY KEY,\n          value JSONB NOT NULL\n      );\n      INSERT INTO public.system_settings (key, value) \n      VALUES ('auto_approve', 'false'::jsonb) \n      ON CONFLICT (key) DO NOTHING;\n\n      -- Dynamic Migration: Change intern role to user in constraints and tables\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'user_profiles' AND column_name = 'role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'user_profiles' AND column_name = 'role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.user_profiles DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.user_profiles SET role = 'user' WHERE role = 'intern';\n              ALTER TABLE public.user_profiles ADD CONSTRAINT user_profiles_role_check CHECK (role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'access_requests' AND column_name = 'requested_role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'access_requests' AND column_name = 'requested_role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.access_requests DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.access_requests SET requested_role = 'user' WHERE requested_role = 'intern';\n              ALTER TABLE public.access_requests ADD CONSTRAINT access_requests_requested_role_check CHECK (requested_role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      -- Uploader By tracking field migrations\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n      ALTER TABLE public.motor_test_runs ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n\n      -- Add main_image and gallery_images to public.motors\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS main_image TEXT;\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS gallery_images JSONB DEFAULT '[]'::jsonb;\n\n      -- Initialize escs and propellers tables\n      CREATE TABLE IF NOT EXISTS public.escs (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_esc_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE TABLE IF NOT EXISTS public.propellers (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_prop_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE INDEX IF NOT EXIST",
    "blocked_mode": "AccessExclusiveLock",
    "blocking_pid": 78134,
    "blocking_query": "SELECT id, role FROM user_profiles WHERE email = $1",
    "blocking_mode": "AccessShareLock"
  },
  {
    "locked_item": "user_profiles",
    "blocked_pid": 78291,
    "blocked_query": "\n      -- Initialize system settings table\n      CREATE TABLE IF NOT EXISTS public.system_settings (\n          key VARCHAR(255) PRIMARY KEY,\n          value JSONB NOT NULL\n      );\n      INSERT INTO public.system_settings (key, value) \n      VALUES ('auto_approve', 'false'::jsonb) \n      ON CONFLICT (key) DO NOTHING;\n\n      -- Dynamic Migration: Change intern role to user in constraints and tables\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'user_profiles' AND column_name = 'role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'user_profiles' AND column_name = 'role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.user_profiles DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.user_profiles SET role = 'user' WHERE role = 'intern';\n              ALTER TABLE public.user_profiles ADD CONSTRAINT user_profiles_role_check CHECK (role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'access_requests' AND column_name = 'requested_role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'access_requests' AND column_name = 'requested_role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.access_requests DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.access_requests SET requested_role = 'user' WHERE requested_role = 'intern';\n              ALTER TABLE public.access_requests ADD CONSTRAINT access_requests_requested_role_check CHECK (requested_role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      -- Uploader By tracking field migrations\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n      ALTER TABLE public.motor_test_runs ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n\n      -- Add main_image and gallery_images to public.motors\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS main_image TEXT;\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS gallery_images JSONB DEFAULT '[]'::jsonb;\n\n      -- Initialize escs and propellers tables\n      CREATE TABLE IF NOT EXISTS public.escs (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_esc_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE TABLE IF NOT EXISTS public.propellers (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_prop_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE INDEX IF NOT EXIST",
    "blocked_mode": "AccessExclusiveLock",
    "blocking_pid": 78300,
    "blocking_query": "SELECT * FROM public.user_profiles ORDER BY email",
    "blocking_mode": "AccessShareLock"
  },
  {
    "locked_item": "motors",
    "blocked_pid": 77960,
    "blocked_query": "SELECT category_id, COUNT(*)::int AS cnt FROM motors GROUP BY category_id",
    "blocked_mode": "AccessShareLock",
    "blocking_pid": 77859,
    "blocking_query": "SELECT max_thrust, recommended_esc, motor_name, custom_parameters FROM motors",
    "blocking_mode": "AccessShareLock"
  },
  {
    "locked_item": "motors",
    "blocked_pid": 77960,
    "blocked_query": "SELECT category_id, COUNT(*)::int AS cnt FROM motors GROUP BY category_id",
    "blocked_mode": "AccessShareLock",
    "blocking_pid": 77918,
    "blocking_query": "\n      -- Initialize system settings table\n      CREATE TABLE IF NOT EXISTS public.system_settings (\n          key VARCHAR(255) PRIMARY KEY,\n          value JSONB NOT NULL\n      );\n      INSERT INTO public.system_settings (key, value) \n      VALUES ('auto_approve', 'false'::jsonb) \n      ON CONFLICT (key) DO NOTHING;\n\n      -- Dynamic Migration: Change intern role to user in constraints and tables\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'user_profiles' AND column_name = 'role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'user_profiles' AND column_name = 'role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.user_profiles DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.user_profiles SET role = 'user' WHERE role = 'intern';\n              ALTER TABLE public.user_profiles ADD CONSTRAINT user_profiles_role_check CHECK (role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'access_requests' AND column_name = 'requested_role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'access_requests' AND column_name = 'requested_role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.access_requests DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.access_requests SET requested_role = 'user' WHERE requested_role = 'intern';\n              ALTER TABLE public.access_requests ADD CONSTRAINT access_requests_requested_role_check CHECK (requested_role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      -- Uploader By tracking field migrations\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n      ALTER TABLE public.motor_test_runs ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n\n      -- Add main_image and gallery_images to public.motors\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS main_image TEXT;\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS gallery_images JSONB DEFAULT '[]'::jsonb;\n\n      -- Initialize escs and propellers tables\n      CREATE TABLE IF NOT EXISTS public.escs (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_esc_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE TABLE IF NOT EXISTS public.propellers (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_prop_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE INDEX IF NOT EXIST",
    "blocking_mode": "AccessExclusiveLock"
  },
  {
    "locked_item": "motors",
    "blocked_pid": 77960,
    "blocked_query": "SELECT category_id, COUNT(*)::int AS cnt FROM motors GROUP BY category_id",
    "blocked_mode": "AccessShareLock",
    "blocking_pid": 77954,
    "blocking_query": "SELECT id, category_id, motor_name, company, max_thrust,\n                         recommended_esc, recommended_propeller,\n                         link_motor, link_esc, link_propeller, custom_parameters, uploaded_by\n                  FROM motors ORDER BY max_thrust ASC LIMIT $1",
    "blocking_mode": "AccessShareLock"
  },
  {
    "locked_item": "motors",
    "blocked_pid": 77960,
    "blocked_query": "SELECT category_id, COUNT(*)::int AS cnt FROM motors GROUP BY category_id",
    "blocked_mode": "AccessShareLock",
    "blocking_pid": 77964,
    "blocking_query": "SELECT DISTINCT company FROM motors WHERE company IS NOT NULL AND company != '' ORDER BY company",
    "blocking_mode": "AccessShareLock"
  },
  {
    "locked_item": "motors",
    "blocked_pid": 77960,
    "blocked_query": "SELECT category_id, COUNT(*)::int AS cnt FROM motors GROUP BY category_id",
    "blocked_mode": "AccessShareLock",
    "blocking_pid": 77959,
    "blocking_query": "SELECT category_id, COUNT(*)::int AS cnt FROM motors GROUP BY category_id",
    "blocking_mode": "AccessShareLock"
  },
  {
    "locked_item": "motors",
    "blocked_pid": 77960,
    "blocked_query": "SELECT category_id, COUNT(*)::int AS cnt FROM motors GROUP BY category_id",
    "blocked_mode": "AccessShareLock",
    "blocking_pid": 77961,
    "blocking_query": "SELECT id, category_id, motor_name, company, max_thrust,\n                         recommended_esc, recommended_propeller,\n                         link_motor, link_esc, link_propeller, custom_parameters, uploaded_by\n                  FROM motors ORDER BY max_thrust ASC LIMIT $1",
    "blocking_mode": "AccessShareLock"
  },
  {
    "locked_item": "motors",
    "blocked_pid": 77960,
    "blocked_query": "SELECT category_id, COUNT(*)::int AS cnt FROM motors GROUP BY category_id",
    "blocked_mode": "AccessShareLock",
    "blocking_pid": 77958,
    "blocking_query": "SELECT DISTINCT company FROM motors WHERE company IS NOT NULL AND company != '' ORDER BY company",
    "blocking_mode": "AccessShareLock"
  },
  {
    "locked_item": "motors",
    "blocked_pid": 77954,
    "blocked_query": "SELECT id, category_id, motor_name, company, max_thrust,\n                         recommended_esc, recommended_propeller,\n                         link_motor, link_esc, link_propeller, custom_parameters, uploaded_by\n                  FROM motors ORDER BY max_thrust ASC LIMIT $1",
    "blocked_mode": "AccessShareLock",
    "blocking_pid": 77859,
    "blocking_query": "SELECT max_thrust, recommended_esc, motor_name, custom_parameters FROM motors",
    "blocking_mode": "AccessShareLock"
  },
  {
    "locked_item": "motors",
    "blocked_pid": 77954,
    "blocked_query": "SELECT id, category_id, motor_name, company, max_thrust,\n                         recommended_esc, recommended_propeller,\n                         link_motor, link_esc, link_propeller, custom_parameters, uploaded_by\n                  FROM motors ORDER BY max_thrust ASC LIMIT $1",
    "blocked_mode": "AccessShareLock",
    "blocking_pid": 77918,
    "blocking_query": "\n      -- Initialize system settings table\n      CREATE TABLE IF NOT EXISTS public.system_settings (\n          key VARCHAR(255) PRIMARY KEY,\n          value JSONB NOT NULL\n      );\n      INSERT INTO public.system_settings (key, value) \n      VALUES ('auto_approve', 'false'::jsonb) \n      ON CONFLICT (key) DO NOTHING;\n\n      -- Dynamic Migration: Change intern role to user in constraints and tables\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'user_profiles' AND column_name = 'role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'user_profiles' AND column_name = 'role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.user_profiles DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.user_profiles SET role = 'user' WHERE role = 'intern';\n              ALTER TABLE public.user_profiles ADD CONSTRAINT user_profiles_role_check CHECK (role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'access_requests' AND column_name = 'requested_role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'access_requests' AND column_name = 'requested_role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.access_requests DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.access_requests SET requested_role = 'user' WHERE requested_role = 'intern';\n              ALTER TABLE public.access_requests ADD CONSTRAINT access_requests_requested_role_check CHECK (requested_role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      -- Uploader By tracking field migrations\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n      ALTER TABLE public.motor_test_runs ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n\n      -- Add main_image and gallery_images to public.motors\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS main_image TEXT;\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS gallery_images JSONB DEFAULT '[]'::jsonb;\n\n      -- Initialize escs and propellers tables\n      CREATE TABLE IF NOT EXISTS public.escs (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_esc_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE TABLE IF NOT EXISTS public.propellers (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_prop_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE INDEX IF NOT EXIST",
    "blocking_mode": "AccessExclusiveLock"
  },
  {
    "locked_item": "motors",
    "blocked_pid": 77954,
    "blocked_query": "SELECT id, category_id, motor_name, company, max_thrust,\n                         recommended_esc, recommended_propeller,\n                         link_motor, link_esc, link_propeller, custom_parameters, uploaded_by\n                  FROM motors ORDER BY max_thrust ASC LIMIT $1",
    "blocked_mode": "AccessShareLock",
    "blocking_pid": 77960,
    "blocking_query": "SELECT category_id, COUNT(*)::int AS cnt FROM motors GROUP BY category_id",
    "blocking_mode": "AccessShareLock"
  },
  {
    "locked_item": "motors",
    "blocked_pid": 77954,
    "blocked_query": "SELECT id, category_id, motor_name, company, max_thrust,\n                         recommended_esc, recommended_propeller,\n                         link_motor, link_esc, link_propeller, custom_parameters, uploaded_by\n                  FROM motors ORDER BY max_thrust ASC LIMIT $1",
    "blocked_mode": "AccessShareLock",
    "blocking_pid": 77964,
    "blocking_query": "SELECT DISTINCT company FROM motors WHERE company IS NOT NULL AND company != '' ORDER BY company",
    "blocking_mode": "AccessShareLock"
  },
  {
    "locked_item": "motors",
    "blocked_pid": 77954,
    "blocked_query": "SELECT id, category_id, motor_name, company, max_thrust,\n                         recommended_esc, recommended_propeller,\n                         link_motor, link_esc, link_propeller, custom_parameters, uploaded_by\n                  FROM motors ORDER BY max_thrust ASC LIMIT $1",
    "blocked_mode": "AccessShareLock",
    "blocking_pid": 77959,
    "blocking_query": "SELECT category_id, COUNT(*)::int AS cnt FROM motors GROUP BY category_id",
    "blocking_mode": "AccessShareLock"
  },
  {
    "locked_item": "motors",
    "blocked_pid": 77954,
    "blocked_query": "SELECT id, category_id, motor_name, company, max_thrust,\n                         recommended_esc, recommended_propeller,\n                         link_motor, link_esc, link_propeller, custom_parameters, uploaded_by\n                  FROM motors ORDER BY max_thrust ASC LIMIT $1",
    "blocked_mode": "AccessShareLock",
    "blocking_pid": 77961,
    "blocking_query": "SELECT id, category_id, motor_name, company, max_thrust,\n                         recommended_esc, recommended_propeller,\n                         link_motor, link_esc, link_propeller, custom_parameters, uploaded_by\n                  FROM motors ORDER BY max_thrust ASC LIMIT $1",
    "blocking_mode": "AccessShareLock"
  },
  {
    "locked_item": "motors",
    "blocked_pid": 77954,
    "blocked_query": "SELECT id, category_id, motor_name, company, max_thrust,\n                         recommended_esc, recommended_propeller,\n                         link_motor, link_esc, link_propeller, custom_parameters, uploaded_by\n                  FROM motors ORDER BY max_thrust ASC LIMIT $1",
    "blocked_mode": "AccessShareLock",
    "blocking_pid": 77958,
    "blocking_query": "SELECT DISTINCT company FROM motors WHERE company IS NOT NULL AND company != '' ORDER BY company",
    "blocking_mode": "AccessShareLock"
  },
  {
    "locked_item": "motors",
    "blocked_pid": 77961,
    "blocked_query": "SELECT id, category_id, motor_name, company, max_thrust,\n                         recommended_esc, recommended_propeller,\n                         link_motor, link_esc, link_propeller, custom_parameters, uploaded_by\n                  FROM motors ORDER BY max_thrust ASC LIMIT $1",
    "blocked_mode": "AccessShareLock",
    "blocking_pid": 77859,
    "blocking_query": "SELECT max_thrust, recommended_esc, motor_name, custom_parameters FROM motors",
    "blocking_mode": "AccessShareLock"
  },
  {
    "locked_item": "motors",
    "blocked_pid": 77961,
    "blocked_query": "SELECT id, category_id, motor_name, company, max_thrust,\n                         recommended_esc, recommended_propeller,\n                         link_motor, link_esc, link_propeller, custom_parameters, uploaded_by\n                  FROM motors ORDER BY max_thrust ASC LIMIT $1",
    "blocked_mode": "AccessShareLock",
    "blocking_pid": 77918,
    "blocking_query": "\n      -- Initialize system settings table\n      CREATE TABLE IF NOT EXISTS public.system_settings (\n          key VARCHAR(255) PRIMARY KEY,\n          value JSONB NOT NULL\n      );\n      INSERT INTO public.system_settings (key, value) \n      VALUES ('auto_approve', 'false'::jsonb) \n      ON CONFLICT (key) DO NOTHING;\n\n      -- Dynamic Migration: Change intern role to user in constraints and tables\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'user_profiles' AND column_name = 'role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'user_profiles' AND column_name = 'role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.user_profiles DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.user_profiles SET role = 'user' WHERE role = 'intern';\n              ALTER TABLE public.user_profiles ADD CONSTRAINT user_profiles_role_check CHECK (role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'access_requests' AND column_name = 'requested_role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'access_requests' AND column_name = 'requested_role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.access_requests DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.access_requests SET requested_role = 'user' WHERE requested_role = 'intern';\n              ALTER TABLE public.access_requests ADD CONSTRAINT access_requests_requested_role_check CHECK (requested_role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      -- Uploader By tracking field migrations\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n      ALTER TABLE public.motor_test_runs ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n\n      -- Add main_image and gallery_images to public.motors\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS main_image TEXT;\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS gallery_images JSONB DEFAULT '[]'::jsonb;\n\n      -- Initialize escs and propellers tables\n      CREATE TABLE IF NOT EXISTS public.escs (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_esc_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE TABLE IF NOT EXISTS public.propellers (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_prop_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE INDEX IF NOT EXIST",
    "blocking_mode": "AccessExclusiveLock"
  },
  {
    "locked_item": "motors",
    "blocked_pid": 77961,
    "blocked_query": "SELECT id, category_id, motor_name, company, max_thrust,\n                         recommended_esc, recommended_propeller,\n                         link_motor, link_esc, link_propeller, custom_parameters, uploaded_by\n                  FROM motors ORDER BY max_thrust ASC LIMIT $1",
    "blocked_mode": "AccessShareLock",
    "blocking_pid": 77960,
    "blocking_query": "SELECT category_id, COUNT(*)::int AS cnt FROM motors GROUP BY category_id",
    "blocking_mode": "AccessShareLock"
  },
  {
    "locked_item": "motors",
    "blocked_pid": 77961,
    "blocked_query": "SELECT id, category_id, motor_name, company, max_thrust,\n                         recommended_esc, recommended_propeller,\n                         link_motor, link_esc, link_propeller, custom_parameters, uploaded_by\n                  FROM motors ORDER BY max_thrust ASC LIMIT $1",
    "blocked_mode": "AccessShareLock",
    "blocking_pid": 77954,
    "blocking_query": "SELECT id, category_id, motor_name, company, max_thrust,\n                         recommended_esc, recommended_propeller,\n                         link_motor, link_esc, link_propeller, custom_parameters, uploaded_by\n                  FROM motors ORDER BY max_thrust ASC LIMIT $1",
    "blocking_mode": "AccessShareLock"
  },
  {
    "locked_item": "motors",
    "blocked_pid": 77961,
    "blocked_query": "SELECT id, category_id, motor_name, company, max_thrust,\n                         recommended_esc, recommended_propeller,\n                         link_motor, link_esc, link_propeller, custom_parameters, uploaded_by\n                  FROM motors ORDER BY max_thrust ASC LIMIT $1",
    "blocked_mode": "AccessShareLock",
    "blocking_pid": 77964,
    "blocking_query": "SELECT DISTINCT company FROM motors WHERE company IS NOT NULL AND company != '' ORDER BY company",
    "blocking_mode": "AccessShareLock"
  },
  {
    "locked_item": "motors",
    "blocked_pid": 77961,
    "blocked_query": "SELECT id, category_id, motor_name, company, max_thrust,\n                         recommended_esc, recommended_propeller,\n                         link_motor, link_esc, link_propeller, custom_parameters, uploaded_by\n                  FROM motors ORDER BY max_thrust ASC LIMIT $1",
    "blocked_mode": "AccessShareLock",
    "blocking_pid": 77959,
    "blocking_query": "SELECT category_id, COUNT(*)::int AS cnt FROM motors GROUP BY category_id",
    "blocking_mode": "AccessShareLock"
  },
  {
    "locked_item": "motors",
    "blocked_pid": 77961,
    "blocked_query": "SELECT id, category_id, motor_name, company, max_thrust,\n                         recommended_esc, recommended_propeller,\n                         link_motor, link_esc, link_propeller, custom_parameters, uploaded_by\n                  FROM motors ORDER BY max_thrust ASC LIMIT $1",
    "blocked_mode": "AccessShareLock",
    "blocking_pid": 77958,
    "blocking_query": "SELECT DISTINCT company FROM motors WHERE company IS NOT NULL AND company != '' ORDER BY company",
    "blocking_mode": "AccessShareLock"
  },
  {
    "locked_item": "motors",
    "blocked_pid": 77918,
    "blocked_query": "\n      -- Initialize system settings table\n      CREATE TABLE IF NOT EXISTS public.system_settings (\n          key VARCHAR(255) PRIMARY KEY,\n          value JSONB NOT NULL\n      );\n      INSERT INTO public.system_settings (key, value) \n      VALUES ('auto_approve', 'false'::jsonb) \n      ON CONFLICT (key) DO NOTHING;\n\n      -- Dynamic Migration: Change intern role to user in constraints and tables\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'user_profiles' AND column_name = 'role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'user_profiles' AND column_name = 'role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.user_profiles DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.user_profiles SET role = 'user' WHERE role = 'intern';\n              ALTER TABLE public.user_profiles ADD CONSTRAINT user_profiles_role_check CHECK (role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'access_requests' AND column_name = 'requested_role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'access_requests' AND column_name = 'requested_role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.access_requests DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.access_requests SET requested_role = 'user' WHERE requested_role = 'intern';\n              ALTER TABLE public.access_requests ADD CONSTRAINT access_requests_requested_role_check CHECK (requested_role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      -- Uploader By tracking field migrations\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n      ALTER TABLE public.motor_test_runs ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n\n      -- Add main_image and gallery_images to public.motors\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS main_image TEXT;\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS gallery_images JSONB DEFAULT '[]'::jsonb;\n\n      -- Initialize escs and propellers tables\n      CREATE TABLE IF NOT EXISTS public.escs (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_esc_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE TABLE IF NOT EXISTS public.propellers (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_prop_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE INDEX IF NOT EXIST",
    "blocked_mode": "AccessExclusiveLock",
    "blocking_pid": 77859,
    "blocking_query": "SELECT max_thrust, recommended_esc, motor_name, custom_parameters FROM motors",
    "blocking_mode": "AccessShareLock"
  },
  {
    "locked_item": "motors",
    "blocked_pid": 77918,
    "blocked_query": "\n      -- Initialize system settings table\n      CREATE TABLE IF NOT EXISTS public.system_settings (\n          key VARCHAR(255) PRIMARY KEY,\n          value JSONB NOT NULL\n      );\n      INSERT INTO public.system_settings (key, value) \n      VALUES ('auto_approve', 'false'::jsonb) \n      ON CONFLICT (key) DO NOTHING;\n\n      -- Dynamic Migration: Change intern role to user in constraints and tables\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'user_profiles' AND column_name = 'role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'user_profiles' AND column_name = 'role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.user_profiles DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.user_profiles SET role = 'user' WHERE role = 'intern';\n              ALTER TABLE public.user_profiles ADD CONSTRAINT user_profiles_role_check CHECK (role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'access_requests' AND column_name = 'requested_role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'access_requests' AND column_name = 'requested_role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.access_requests DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.access_requests SET requested_role = 'user' WHERE requested_role = 'intern';\n              ALTER TABLE public.access_requests ADD CONSTRAINT access_requests_requested_role_check CHECK (requested_role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      -- Uploader By tracking field migrations\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n      ALTER TABLE public.motor_test_runs ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n\n      -- Add main_image and gallery_images to public.motors\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS main_image TEXT;\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS gallery_images JSONB DEFAULT '[]'::jsonb;\n\n      -- Initialize escs and propellers tables\n      CREATE TABLE IF NOT EXISTS public.escs (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_esc_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE TABLE IF NOT EXISTS public.propellers (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_prop_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE INDEX IF NOT EXIST",
    "blocked_mode": "AccessExclusiveLock",
    "blocking_pid": 77960,
    "blocking_query": "SELECT category_id, COUNT(*)::int AS cnt FROM motors GROUP BY category_id",
    "blocking_mode": "AccessShareLock"
  },
  {
    "locked_item": "motors",
    "blocked_pid": 77918,
    "blocked_query": "\n      -- Initialize system settings table\n      CREATE TABLE IF NOT EXISTS public.system_settings (\n          key VARCHAR(255) PRIMARY KEY,\n          value JSONB NOT NULL\n      );\n      INSERT INTO public.system_settings (key, value) \n      VALUES ('auto_approve', 'false'::jsonb) \n      ON CONFLICT (key) DO NOTHING;\n\n      -- Dynamic Migration: Change intern role to user in constraints and tables\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'user_profiles' AND column_name = 'role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'user_profiles' AND column_name = 'role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.user_profiles DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.user_profiles SET role = 'user' WHERE role = 'intern';\n              ALTER TABLE public.user_profiles ADD CONSTRAINT user_profiles_role_check CHECK (role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'access_requests' AND column_name = 'requested_role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'access_requests' AND column_name = 'requested_role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.access_requests DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.access_requests SET requested_role = 'user' WHERE requested_role = 'intern';\n              ALTER TABLE public.access_requests ADD CONSTRAINT access_requests_requested_role_check CHECK (requested_role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      -- Uploader By tracking field migrations\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n      ALTER TABLE public.motor_test_runs ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n\n      -- Add main_image and gallery_images to public.motors\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS main_image TEXT;\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS gallery_images JSONB DEFAULT '[]'::jsonb;\n\n      -- Initialize escs and propellers tables\n      CREATE TABLE IF NOT EXISTS public.escs (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_esc_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE TABLE IF NOT EXISTS public.propellers (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_prop_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE INDEX IF NOT EXIST",
    "blocked_mode": "AccessExclusiveLock",
    "blocking_pid": 77954,
    "blocking_query": "SELECT id, category_id, motor_name, company, max_thrust,\n                         recommended_esc, recommended_propeller,\n                         link_motor, link_esc, link_propeller, custom_parameters, uploaded_by\n                  FROM motors ORDER BY max_thrust ASC LIMIT $1",
    "blocking_mode": "AccessShareLock"
  },
  {
    "locked_item": "motors",
    "blocked_pid": 77918,
    "blocked_query": "\n      -- Initialize system settings table\n      CREATE TABLE IF NOT EXISTS public.system_settings (\n          key VARCHAR(255) PRIMARY KEY,\n          value JSONB NOT NULL\n      );\n      INSERT INTO public.system_settings (key, value) \n      VALUES ('auto_approve', 'false'::jsonb) \n      ON CONFLICT (key) DO NOTHING;\n\n      -- Dynamic Migration: Change intern role to user in constraints and tables\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'user_profiles' AND column_name = 'role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'user_profiles' AND column_name = 'role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.user_profiles DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.user_profiles SET role = 'user' WHERE role = 'intern';\n              ALTER TABLE public.user_profiles ADD CONSTRAINT user_profiles_role_check CHECK (role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'access_requests' AND column_name = 'requested_role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'access_requests' AND column_name = 'requested_role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.access_requests DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.access_requests SET requested_role = 'user' WHERE requested_role = 'intern';\n              ALTER TABLE public.access_requests ADD CONSTRAINT access_requests_requested_role_check CHECK (requested_role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      -- Uploader By tracking field migrations\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n      ALTER TABLE public.motor_test_runs ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n\n      -- Add main_image and gallery_images to public.motors\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS main_image TEXT;\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS gallery_images JSONB DEFAULT '[]'::jsonb;\n\n      -- Initialize escs and propellers tables\n      CREATE TABLE IF NOT EXISTS public.escs (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_esc_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE TABLE IF NOT EXISTS public.propellers (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_prop_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE INDEX IF NOT EXIST",
    "blocked_mode": "AccessExclusiveLock",
    "blocking_pid": 77964,
    "blocking_query": "SELECT DISTINCT company FROM motors WHERE company IS NOT NULL AND company != '' ORDER BY company",
    "blocking_mode": "AccessShareLock"
  },
  {
    "locked_item": "motors",
    "blocked_pid": 77918,
    "blocked_query": "\n      -- Initialize system settings table\n      CREATE TABLE IF NOT EXISTS public.system_settings (\n          key VARCHAR(255) PRIMARY KEY,\n          value JSONB NOT NULL\n      );\n      INSERT INTO public.system_settings (key, value) \n      VALUES ('auto_approve', 'false'::jsonb) \n      ON CONFLICT (key) DO NOTHING;\n\n      -- Dynamic Migration: Change intern role to user in constraints and tables\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'user_profiles' AND column_name = 'role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'user_profiles' AND column_name = 'role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.user_profiles DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.user_profiles SET role = 'user' WHERE role = 'intern';\n              ALTER TABLE public.user_profiles ADD CONSTRAINT user_profiles_role_check CHECK (role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'access_requests' AND column_name = 'requested_role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'access_requests' AND column_name = 'requested_role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.access_requests DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.access_requests SET requested_role = 'user' WHERE requested_role = 'intern';\n              ALTER TABLE public.access_requests ADD CONSTRAINT access_requests_requested_role_check CHECK (requested_role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      -- Uploader By tracking field migrations\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n      ALTER TABLE public.motor_test_runs ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n\n      -- Add main_image and gallery_images to public.motors\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS main_image TEXT;\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS gallery_images JSONB DEFAULT '[]'::jsonb;\n\n      -- Initialize escs and propellers tables\n      CREATE TABLE IF NOT EXISTS public.escs (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_esc_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE TABLE IF NOT EXISTS public.propellers (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_prop_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE INDEX IF NOT EXIST",
    "blocked_mode": "AccessExclusiveLock",
    "blocking_pid": 77959,
    "blocking_query": "SELECT category_id, COUNT(*)::int AS cnt FROM motors GROUP BY category_id",
    "blocking_mode": "AccessShareLock"
  },
  {
    "locked_item": "motors",
    "blocked_pid": 77918,
    "blocked_query": "\n      -- Initialize system settings table\n      CREATE TABLE IF NOT EXISTS public.system_settings (\n          key VARCHAR(255) PRIMARY KEY,\n          value JSONB NOT NULL\n      );\n      INSERT INTO public.system_settings (key, value) \n      VALUES ('auto_approve', 'false'::jsonb) \n      ON CONFLICT (key) DO NOTHING;\n\n      -- Dynamic Migration: Change intern role to user in constraints and tables\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'user_profiles' AND column_name = 'role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'user_profiles' AND column_name = 'role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.user_profiles DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.user_profiles SET role = 'user' WHERE role = 'intern';\n              ALTER TABLE public.user_profiles ADD CONSTRAINT user_profiles_role_check CHECK (role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'access_requests' AND column_name = 'requested_role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'access_requests' AND column_name = 'requested_role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.access_requests DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.access_requests SET requested_role = 'user' WHERE requested_role = 'intern';\n              ALTER TABLE public.access_requests ADD CONSTRAINT access_requests_requested_role_check CHECK (requested_role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      -- Uploader By tracking field migrations\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n      ALTER TABLE public.motor_test_runs ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n\n      -- Add main_image and gallery_images to public.motors\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS main_image TEXT;\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS gallery_images JSONB DEFAULT '[]'::jsonb;\n\n      -- Initialize escs and propellers tables\n      CREATE TABLE IF NOT EXISTS public.escs (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_esc_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE TABLE IF NOT EXISTS public.propellers (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_prop_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE INDEX IF NOT EXIST",
    "blocked_mode": "AccessExclusiveLock",
    "blocking_pid": 77961,
    "blocking_query": "SELECT id, category_id, motor_name, company, max_thrust,\n                         recommended_esc, recommended_propeller,\n                         link_motor, link_esc, link_propeller, custom_parameters, uploaded_by\n                  FROM motors ORDER BY max_thrust ASC LIMIT $1",
    "blocking_mode": "AccessShareLock"
  },
  {
    "locked_item": "motors",
    "blocked_pid": 77918,
    "blocked_query": "\n      -- Initialize system settings table\n      CREATE TABLE IF NOT EXISTS public.system_settings (\n          key VARCHAR(255) PRIMARY KEY,\n          value JSONB NOT NULL\n      );\n      INSERT INTO public.system_settings (key, value) \n      VALUES ('auto_approve', 'false'::jsonb) \n      ON CONFLICT (key) DO NOTHING;\n\n      -- Dynamic Migration: Change intern role to user in constraints and tables\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'user_profiles' AND column_name = 'role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'user_profiles' AND column_name = 'role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.user_profiles DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.user_profiles SET role = 'user' WHERE role = 'intern';\n              ALTER TABLE public.user_profiles ADD CONSTRAINT user_profiles_role_check CHECK (role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'access_requests' AND column_name = 'requested_role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'access_requests' AND column_name = 'requested_role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.access_requests DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.access_requests SET requested_role = 'user' WHERE requested_role = 'intern';\n              ALTER TABLE public.access_requests ADD CONSTRAINT access_requests_requested_role_check CHECK (requested_role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      -- Uploader By tracking field migrations\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n      ALTER TABLE public.motor_test_runs ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n\n      -- Add main_image and gallery_images to public.motors\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS main_image TEXT;\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS gallery_images JSONB DEFAULT '[]'::jsonb;\n\n      -- Initialize escs and propellers tables\n      CREATE TABLE IF NOT EXISTS public.escs (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_esc_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE TABLE IF NOT EXISTS public.propellers (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_prop_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE INDEX IF NOT EXIST",
    "blocked_mode": "AccessExclusiveLock",
    "blocking_pid": 77958,
    "blocking_query": "SELECT DISTINCT company FROM motors WHERE company IS NOT NULL AND company != '' ORDER BY company",
    "blocking_mode": "AccessShareLock"
  },
  {
    "locked_item": "user_profiles",
    "blocked_pid": 78164,
    "blocked_query": "\n      -- Initialize system settings table\n      CREATE TABLE IF NOT EXISTS public.system_settings (\n          key VARCHAR(255) PRIMARY KEY,\n          value JSONB NOT NULL\n      );\n      INSERT INTO public.system_settings (key, value) \n      VALUES ('auto_approve', 'false'::jsonb) \n      ON CONFLICT (key) DO NOTHING;\n\n      -- Dynamic Migration: Change intern role to user in constraints and tables\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'user_profiles' AND column_name = 'role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'user_profiles' AND column_name = 'role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.user_profiles DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.user_profiles SET role = 'user' WHERE role = 'intern';\n              ALTER TABLE public.user_profiles ADD CONSTRAINT user_profiles_role_check CHECK (role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'access_requests' AND column_name = 'requested_role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'access_requests' AND column_name = 'requested_role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.access_requests DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.access_requests SET requested_role = 'user' WHERE requested_role = 'intern';\n              ALTER TABLE public.access_requests ADD CONSTRAINT access_requests_requested_role_check CHECK (requested_role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      -- Uploader By tracking field migrations\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n      ALTER TABLE public.motor_test_runs ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n\n      -- Add main_image and gallery_images to public.motors\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS main_image TEXT;\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS gallery_images JSONB DEFAULT '[]'::jsonb;\n\n      -- Initialize escs and propellers tables\n      CREATE TABLE IF NOT EXISTS public.escs (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_esc_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE TABLE IF NOT EXISTS public.propellers (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_prop_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE INDEX IF NOT EXIST",
    "blocked_mode": "AccessExclusiveLock",
    "blocking_pid": 77918,
    "blocking_query": "\n      -- Initialize system settings table\n      CREATE TABLE IF NOT EXISTS public.system_settings (\n          key VARCHAR(255) PRIMARY KEY,\n          value JSONB NOT NULL\n      );\n      INSERT INTO public.system_settings (key, value) \n      VALUES ('auto_approve', 'false'::jsonb) \n      ON CONFLICT (key) DO NOTHING;\n\n      -- Dynamic Migration: Change intern role to user in constraints and tables\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'user_profiles' AND column_name = 'role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'user_profiles' AND column_name = 'role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.user_profiles DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.user_profiles SET role = 'user' WHERE role = 'intern';\n              ALTER TABLE public.user_profiles ADD CONSTRAINT user_profiles_role_check CHECK (role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'access_requests' AND column_name = 'requested_role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'access_requests' AND column_name = 'requested_role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.access_requests DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.access_requests SET requested_role = 'user' WHERE requested_role = 'intern';\n              ALTER TABLE public.access_requests ADD CONSTRAINT access_requests_requested_role_check CHECK (requested_role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      -- Uploader By tracking field migrations\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n      ALTER TABLE public.motor_test_runs ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n\n      -- Add main_image and gallery_images to public.motors\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS main_image TEXT;\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS gallery_images JSONB DEFAULT '[]'::jsonb;\n\n      -- Initialize escs and propellers tables\n      CREATE TABLE IF NOT EXISTS public.escs (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_esc_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE TABLE IF NOT EXISTS public.propellers (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_prop_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE INDEX IF NOT EXIST",
    "blocking_mode": "AccessExclusiveLock"
  },
  {
    "locked_item": "user_profiles",
    "blocked_pid": 78164,
    "blocked_query": "\n      -- Initialize system settings table\n      CREATE TABLE IF NOT EXISTS public.system_settings (\n          key VARCHAR(255) PRIMARY KEY,\n          value JSONB NOT NULL\n      );\n      INSERT INTO public.system_settings (key, value) \n      VALUES ('auto_approve', 'false'::jsonb) \n      ON CONFLICT (key) DO NOTHING;\n\n      -- Dynamic Migration: Change intern role to user in constraints and tables\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'user_profiles' AND column_name = 'role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'user_profiles' AND column_name = 'role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.user_profiles DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.user_profiles SET role = 'user' WHERE role = 'intern';\n              ALTER TABLE public.user_profiles ADD CONSTRAINT user_profiles_role_check CHECK (role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'access_requests' AND column_name = 'requested_role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'access_requests' AND column_name = 'requested_role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.access_requests DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.access_requests SET requested_role = 'user' WHERE requested_role = 'intern';\n              ALTER TABLE public.access_requests ADD CONSTRAINT access_requests_requested_role_check CHECK (requested_role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      -- Uploader By tracking field migrations\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n      ALTER TABLE public.motor_test_runs ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n\n      -- Add main_image and gallery_images to public.motors\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS main_image TEXT;\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS gallery_images JSONB DEFAULT '[]'::jsonb;\n\n      -- Initialize escs and propellers tables\n      CREATE TABLE IF NOT EXISTS public.escs (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_esc_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE TABLE IF NOT EXISTS public.propellers (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_prop_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE INDEX IF NOT EXIST",
    "blocked_mode": "AccessExclusiveLock",
    "blocking_pid": 77918,
    "blocking_query": "\n      -- Initialize system settings table\n      CREATE TABLE IF NOT EXISTS public.system_settings (\n          key VARCHAR(255) PRIMARY KEY,\n          value JSONB NOT NULL\n      );\n      INSERT INTO public.system_settings (key, value) \n      VALUES ('auto_approve', 'false'::jsonb) \n      ON CONFLICT (key) DO NOTHING;\n\n      -- Dynamic Migration: Change intern role to user in constraints and tables\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'user_profiles' AND column_name = 'role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'user_profiles' AND column_name = 'role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.user_profiles DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.user_profiles SET role = 'user' WHERE role = 'intern';\n              ALTER TABLE public.user_profiles ADD CONSTRAINT user_profiles_role_check CHECK (role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'access_requests' AND column_name = 'requested_role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'access_requests' AND column_name = 'requested_role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.access_requests DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.access_requests SET requested_role = 'user' WHERE requested_role = 'intern';\n              ALTER TABLE public.access_requests ADD CONSTRAINT access_requests_requested_role_check CHECK (requested_role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      -- Uploader By tracking field migrations\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n      ALTER TABLE public.motor_test_runs ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n\n      -- Add main_image and gallery_images to public.motors\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS main_image TEXT;\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS gallery_images JSONB DEFAULT '[]'::jsonb;\n\n      -- Initialize escs and propellers tables\n      CREATE TABLE IF NOT EXISTS public.escs (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_esc_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE TABLE IF NOT EXISTS public.propellers (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_prop_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE INDEX IF NOT EXIST",
    "blocking_mode": "RowExclusiveLock"
  },
  {
    "locked_item": "user_profiles",
    "blocked_pid": 78164,
    "blocked_query": "\n      -- Initialize system settings table\n      CREATE TABLE IF NOT EXISTS public.system_settings (\n          key VARCHAR(255) PRIMARY KEY,\n          value JSONB NOT NULL\n      );\n      INSERT INTO public.system_settings (key, value) \n      VALUES ('auto_approve', 'false'::jsonb) \n      ON CONFLICT (key) DO NOTHING;\n\n      -- Dynamic Migration: Change intern role to user in constraints and tables\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'user_profiles' AND column_name = 'role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'user_profiles' AND column_name = 'role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.user_profiles DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.user_profiles SET role = 'user' WHERE role = 'intern';\n              ALTER TABLE public.user_profiles ADD CONSTRAINT user_profiles_role_check CHECK (role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'access_requests' AND column_name = 'requested_role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'access_requests' AND column_name = 'requested_role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.access_requests DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.access_requests SET requested_role = 'user' WHERE requested_role = 'intern';\n              ALTER TABLE public.access_requests ADD CONSTRAINT access_requests_requested_role_check CHECK (requested_role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      -- Uploader By tracking field migrations\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n      ALTER TABLE public.motor_test_runs ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n\n      -- Add main_image and gallery_images to public.motors\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS main_image TEXT;\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS gallery_images JSONB DEFAULT '[]'::jsonb;\n\n      -- Initialize escs and propellers tables\n      CREATE TABLE IF NOT EXISTS public.escs (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_esc_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE TABLE IF NOT EXISTS public.propellers (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_prop_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE INDEX IF NOT EXIST",
    "blocked_mode": "AccessExclusiveLock",
    "blocking_pid": 77970,
    "blocking_query": "SELECT id, role FROM user_profiles WHERE email = $1",
    "blocking_mode": "AccessShareLock"
  },
  {
    "locked_item": "user_profiles",
    "blocked_pid": 78164,
    "blocked_query": "\n      -- Initialize system settings table\n      CREATE TABLE IF NOT EXISTS public.system_settings (\n          key VARCHAR(255) PRIMARY KEY,\n          value JSONB NOT NULL\n      );\n      INSERT INTO public.system_settings (key, value) \n      VALUES ('auto_approve', 'false'::jsonb) \n      ON CONFLICT (key) DO NOTHING;\n\n      -- Dynamic Migration: Change intern role to user in constraints and tables\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'user_profiles' AND column_name = 'role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'user_profiles' AND column_name = 'role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.user_profiles DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.user_profiles SET role = 'user' WHERE role = 'intern';\n              ALTER TABLE public.user_profiles ADD CONSTRAINT user_profiles_role_check CHECK (role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'access_requests' AND column_name = 'requested_role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'access_requests' AND column_name = 'requested_role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.access_requests DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.access_requests SET requested_role = 'user' WHERE requested_role = 'intern';\n              ALTER TABLE public.access_requests ADD CONSTRAINT access_requests_requested_role_check CHECK (requested_role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      -- Uploader By tracking field migrations\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n      ALTER TABLE public.motor_test_runs ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n\n      -- Add main_image and gallery_images to public.motors\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS main_image TEXT;\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS gallery_images JSONB DEFAULT '[]'::jsonb;\n\n      -- Initialize escs and propellers tables\n      CREATE TABLE IF NOT EXISTS public.escs (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_esc_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE TABLE IF NOT EXISTS public.propellers (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_prop_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE INDEX IF NOT EXIST",
    "blocked_mode": "AccessExclusiveLock",
    "blocking_pid": 78291,
    "blocking_query": "\n      -- Initialize system settings table\n      CREATE TABLE IF NOT EXISTS public.system_settings (\n          key VARCHAR(255) PRIMARY KEY,\n          value JSONB NOT NULL\n      );\n      INSERT INTO public.system_settings (key, value) \n      VALUES ('auto_approve', 'false'::jsonb) \n      ON CONFLICT (key) DO NOTHING;\n\n      -- Dynamic Migration: Change intern role to user in constraints and tables\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'user_profiles' AND column_name = 'role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'user_profiles' AND column_name = 'role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.user_profiles DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.user_profiles SET role = 'user' WHERE role = 'intern';\n              ALTER TABLE public.user_profiles ADD CONSTRAINT user_profiles_role_check CHECK (role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'access_requests' AND column_name = 'requested_role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'access_requests' AND column_name = 'requested_role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.access_requests DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.access_requests SET requested_role = 'user' WHERE requested_role = 'intern';\n              ALTER TABLE public.access_requests ADD CONSTRAINT access_requests_requested_role_check CHECK (requested_role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      -- Uploader By tracking field migrations\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n      ALTER TABLE public.motor_test_runs ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n\n      -- Add main_image and gallery_images to public.motors\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS main_image TEXT;\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS gallery_images JSONB DEFAULT '[]'::jsonb;\n\n      -- Initialize escs and propellers tables\n      CREATE TABLE IF NOT EXISTS public.escs (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_esc_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE TABLE IF NOT EXISTS public.propellers (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_prop_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE INDEX IF NOT EXIST",
    "blocking_mode": "AccessExclusiveLock"
  },
  {
    "locked_item": "user_profiles",
    "blocked_pid": 78164,
    "blocked_query": "\n      -- Initialize system settings table\n      CREATE TABLE IF NOT EXISTS public.system_settings (\n          key VARCHAR(255) PRIMARY KEY,\n          value JSONB NOT NULL\n      );\n      INSERT INTO public.system_settings (key, value) \n      VALUES ('auto_approve', 'false'::jsonb) \n      ON CONFLICT (key) DO NOTHING;\n\n      -- Dynamic Migration: Change intern role to user in constraints and tables\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'user_profiles' AND column_name = 'role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'user_profiles' AND column_name = 'role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.user_profiles DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.user_profiles SET role = 'user' WHERE role = 'intern';\n              ALTER TABLE public.user_profiles ADD CONSTRAINT user_profiles_role_check CHECK (role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'access_requests' AND column_name = 'requested_role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'access_requests' AND column_name = 'requested_role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.access_requests DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.access_requests SET requested_role = 'user' WHERE requested_role = 'intern';\n              ALTER TABLE public.access_requests ADD CONSTRAINT access_requests_requested_role_check CHECK (requested_role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      -- Uploader By tracking field migrations\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n      ALTER TABLE public.motor_test_runs ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n\n      -- Add main_image and gallery_images to public.motors\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS main_image TEXT;\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS gallery_images JSONB DEFAULT '[]'::jsonb;\n\n      -- Initialize escs and propellers tables\n      CREATE TABLE IF NOT EXISTS public.escs (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_esc_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE TABLE IF NOT EXISTS public.propellers (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_prop_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE INDEX IF NOT EXIST",
    "blocked_mode": "AccessExclusiveLock",
    "blocking_pid": 78134,
    "blocking_query": "SELECT id, role FROM user_profiles WHERE email = $1",
    "blocking_mode": "AccessShareLock"
  },
  {
    "locked_item": "user_profiles",
    "blocked_pid": 78164,
    "blocked_query": "\n      -- Initialize system settings table\n      CREATE TABLE IF NOT EXISTS public.system_settings (\n          key VARCHAR(255) PRIMARY KEY,\n          value JSONB NOT NULL\n      );\n      INSERT INTO public.system_settings (key, value) \n      VALUES ('auto_approve', 'false'::jsonb) \n      ON CONFLICT (key) DO NOTHING;\n\n      -- Dynamic Migration: Change intern role to user in constraints and tables\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'user_profiles' AND column_name = 'role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'user_profiles' AND column_name = 'role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.user_profiles DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.user_profiles SET role = 'user' WHERE role = 'intern';\n              ALTER TABLE public.user_profiles ADD CONSTRAINT user_profiles_role_check CHECK (role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'access_requests' AND column_name = 'requested_role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'access_requests' AND column_name = 'requested_role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.access_requests DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.access_requests SET requested_role = 'user' WHERE requested_role = 'intern';\n              ALTER TABLE public.access_requests ADD CONSTRAINT access_requests_requested_role_check CHECK (requested_role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      -- Uploader By tracking field migrations\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n      ALTER TABLE public.motor_test_runs ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n\n      -- Add main_image and gallery_images to public.motors\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS main_image TEXT;\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS gallery_images JSONB DEFAULT '[]'::jsonb;\n\n      -- Initialize escs and propellers tables\n      CREATE TABLE IF NOT EXISTS public.escs (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_esc_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE TABLE IF NOT EXISTS public.propellers (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_prop_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE INDEX IF NOT EXIST",
    "blocked_mode": "AccessExclusiveLock",
    "blocking_pid": 78300,
    "blocking_query": "SELECT * FROM public.user_profiles ORDER BY email",
    "blocking_mode": "AccessShareLock"
  },
  {
    "locked_item": "motors",
    "blocked_pid": 77959,
    "blocked_query": "SELECT category_id, COUNT(*)::int AS cnt FROM motors GROUP BY category_id",
    "blocked_mode": "AccessShareLock",
    "blocking_pid": 77859,
    "blocking_query": "SELECT max_thrust, recommended_esc, motor_name, custom_parameters FROM motors",
    "blocking_mode": "AccessShareLock"
  },
  {
    "locked_item": "motors",
    "blocked_pid": 77959,
    "blocked_query": "SELECT category_id, COUNT(*)::int AS cnt FROM motors GROUP BY category_id",
    "blocked_mode": "AccessShareLock",
    "blocking_pid": 77918,
    "blocking_query": "\n      -- Initialize system settings table\n      CREATE TABLE IF NOT EXISTS public.system_settings (\n          key VARCHAR(255) PRIMARY KEY,\n          value JSONB NOT NULL\n      );\n      INSERT INTO public.system_settings (key, value) \n      VALUES ('auto_approve', 'false'::jsonb) \n      ON CONFLICT (key) DO NOTHING;\n\n      -- Dynamic Migration: Change intern role to user in constraints and tables\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'user_profiles' AND column_name = 'role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'user_profiles' AND column_name = 'role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.user_profiles DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.user_profiles SET role = 'user' WHERE role = 'intern';\n              ALTER TABLE public.user_profiles ADD CONSTRAINT user_profiles_role_check CHECK (role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'access_requests' AND column_name = 'requested_role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'access_requests' AND column_name = 'requested_role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.access_requests DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.access_requests SET requested_role = 'user' WHERE requested_role = 'intern';\n              ALTER TABLE public.access_requests ADD CONSTRAINT access_requests_requested_role_check CHECK (requested_role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      -- Uploader By tracking field migrations\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n      ALTER TABLE public.motor_test_runs ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n\n      -- Add main_image and gallery_images to public.motors\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS main_image TEXT;\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS gallery_images JSONB DEFAULT '[]'::jsonb;\n\n      -- Initialize escs and propellers tables\n      CREATE TABLE IF NOT EXISTS public.escs (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_esc_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE TABLE IF NOT EXISTS public.propellers (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_prop_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE INDEX IF NOT EXIST",
    "blocking_mode": "AccessExclusiveLock"
  },
  {
    "locked_item": "motors",
    "blocked_pid": 77959,
    "blocked_query": "SELECT category_id, COUNT(*)::int AS cnt FROM motors GROUP BY category_id",
    "blocked_mode": "AccessShareLock",
    "blocking_pid": 77960,
    "blocking_query": "SELECT category_id, COUNT(*)::int AS cnt FROM motors GROUP BY category_id",
    "blocking_mode": "AccessShareLock"
  },
  {
    "locked_item": "motors",
    "blocked_pid": 77959,
    "blocked_query": "SELECT category_id, COUNT(*)::int AS cnt FROM motors GROUP BY category_id",
    "blocked_mode": "AccessShareLock",
    "blocking_pid": 77954,
    "blocking_query": "SELECT id, category_id, motor_name, company, max_thrust,\n                         recommended_esc, recommended_propeller,\n                         link_motor, link_esc, link_propeller, custom_parameters, uploaded_by\n                  FROM motors ORDER BY max_thrust ASC LIMIT $1",
    "blocking_mode": "AccessShareLock"
  },
  {
    "locked_item": "motors",
    "blocked_pid": 77959,
    "blocked_query": "SELECT category_id, COUNT(*)::int AS cnt FROM motors GROUP BY category_id",
    "blocked_mode": "AccessShareLock",
    "blocking_pid": 77964,
    "blocking_query": "SELECT DISTINCT company FROM motors WHERE company IS NOT NULL AND company != '' ORDER BY company",
    "blocking_mode": "AccessShareLock"
  },
  {
    "locked_item": "motors",
    "blocked_pid": 77959,
    "blocked_query": "SELECT category_id, COUNT(*)::int AS cnt FROM motors GROUP BY category_id",
    "blocked_mode": "AccessShareLock",
    "blocking_pid": 77961,
    "blocking_query": "SELECT id, category_id, motor_name, company, max_thrust,\n                         recommended_esc, recommended_propeller,\n                         link_motor, link_esc, link_propeller, custom_parameters, uploaded_by\n                  FROM motors ORDER BY max_thrust ASC LIMIT $1",
    "blocking_mode": "AccessShareLock"
  },
  {
    "locked_item": "motors",
    "blocked_pid": 77959,
    "blocked_query": "SELECT category_id, COUNT(*)::int AS cnt FROM motors GROUP BY category_id",
    "blocked_mode": "AccessShareLock",
    "blocking_pid": 77958,
    "blocking_query": "SELECT DISTINCT company FROM motors WHERE company IS NOT NULL AND company != '' ORDER BY company",
    "blocking_mode": "AccessShareLock"
  },
  {
    "locked_item": "user_profiles",
    "blocked_pid": 78300,
    "blocked_query": "SELECT * FROM public.user_profiles ORDER BY email",
    "blocked_mode": "AccessShareLock",
    "blocking_pid": 77918,
    "blocking_query": "\n      -- Initialize system settings table\n      CREATE TABLE IF NOT EXISTS public.system_settings (\n          key VARCHAR(255) PRIMARY KEY,\n          value JSONB NOT NULL\n      );\n      INSERT INTO public.system_settings (key, value) \n      VALUES ('auto_approve', 'false'::jsonb) \n      ON CONFLICT (key) DO NOTHING;\n\n      -- Dynamic Migration: Change intern role to user in constraints and tables\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'user_profiles' AND column_name = 'role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'user_profiles' AND column_name = 'role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.user_profiles DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.user_profiles SET role = 'user' WHERE role = 'intern';\n              ALTER TABLE public.user_profiles ADD CONSTRAINT user_profiles_role_check CHECK (role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'access_requests' AND column_name = 'requested_role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'access_requests' AND column_name = 'requested_role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.access_requests DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.access_requests SET requested_role = 'user' WHERE requested_role = 'intern';\n              ALTER TABLE public.access_requests ADD CONSTRAINT access_requests_requested_role_check CHECK (requested_role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      -- Uploader By tracking field migrations\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n      ALTER TABLE public.motor_test_runs ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n\n      -- Add main_image and gallery_images to public.motors\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS main_image TEXT;\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS gallery_images JSONB DEFAULT '[]'::jsonb;\n\n      -- Initialize escs and propellers tables\n      CREATE TABLE IF NOT EXISTS public.escs (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_esc_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE TABLE IF NOT EXISTS public.propellers (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_prop_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE INDEX IF NOT EXIST",
    "blocking_mode": "AccessExclusiveLock"
  },
  {
    "locked_item": "user_profiles",
    "blocked_pid": 78300,
    "blocked_query": "SELECT * FROM public.user_profiles ORDER BY email",
    "blocked_mode": "AccessShareLock",
    "blocking_pid": 77918,
    "blocking_query": "\n      -- Initialize system settings table\n      CREATE TABLE IF NOT EXISTS public.system_settings (\n          key VARCHAR(255) PRIMARY KEY,\n          value JSONB NOT NULL\n      );\n      INSERT INTO public.system_settings (key, value) \n      VALUES ('auto_approve', 'false'::jsonb) \n      ON CONFLICT (key) DO NOTHING;\n\n      -- Dynamic Migration: Change intern role to user in constraints and tables\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'user_profiles' AND column_name = 'role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'user_profiles' AND column_name = 'role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.user_profiles DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.user_profiles SET role = 'user' WHERE role = 'intern';\n              ALTER TABLE public.user_profiles ADD CONSTRAINT user_profiles_role_check CHECK (role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'access_requests' AND column_name = 'requested_role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'access_requests' AND column_name = 'requested_role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.access_requests DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.access_requests SET requested_role = 'user' WHERE requested_role = 'intern';\n              ALTER TABLE public.access_requests ADD CONSTRAINT access_requests_requested_role_check CHECK (requested_role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      -- Uploader By tracking field migrations\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n      ALTER TABLE public.motor_test_runs ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n\n      -- Add main_image and gallery_images to public.motors\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS main_image TEXT;\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS gallery_images JSONB DEFAULT '[]'::jsonb;\n\n      -- Initialize escs and propellers tables\n      CREATE TABLE IF NOT EXISTS public.escs (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_esc_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE TABLE IF NOT EXISTS public.propellers (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_prop_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE INDEX IF NOT EXIST",
    "blocking_mode": "RowExclusiveLock"
  },
  {
    "locked_item": "user_profiles",
    "blocked_pid": 78300,
    "blocked_query": "SELECT * FROM public.user_profiles ORDER BY email",
    "blocked_mode": "AccessShareLock",
    "blocking_pid": 77970,
    "blocking_query": "SELECT id, role FROM user_profiles WHERE email = $1",
    "blocking_mode": "AccessShareLock"
  },
  {
    "locked_item": "user_profiles",
    "blocked_pid": 78300,
    "blocked_query": "SELECT * FROM public.user_profiles ORDER BY email",
    "blocked_mode": "AccessShareLock",
    "blocking_pid": 78164,
    "blocking_query": "\n      -- Initialize system settings table\n      CREATE TABLE IF NOT EXISTS public.system_settings (\n          key VARCHAR(255) PRIMARY KEY,\n          value JSONB NOT NULL\n      );\n      INSERT INTO public.system_settings (key, value) \n      VALUES ('auto_approve', 'false'::jsonb) \n      ON CONFLICT (key) DO NOTHING;\n\n      -- Dynamic Migration: Change intern role to user in constraints and tables\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'user_profiles' AND column_name = 'role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'user_profiles' AND column_name = 'role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.user_profiles DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.user_profiles SET role = 'user' WHERE role = 'intern';\n              ALTER TABLE public.user_profiles ADD CONSTRAINT user_profiles_role_check CHECK (role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'access_requests' AND column_name = 'requested_role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'access_requests' AND column_name = 'requested_role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.access_requests DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.access_requests SET requested_role = 'user' WHERE requested_role = 'intern';\n              ALTER TABLE public.access_requests ADD CONSTRAINT access_requests_requested_role_check CHECK (requested_role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      -- Uploader By tracking field migrations\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n      ALTER TABLE public.motor_test_runs ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n\n      -- Add main_image and gallery_images to public.motors\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS main_image TEXT;\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS gallery_images JSONB DEFAULT '[]'::jsonb;\n\n      -- Initialize escs and propellers tables\n      CREATE TABLE IF NOT EXISTS public.escs (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_esc_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE TABLE IF NOT EXISTS public.propellers (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_prop_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE INDEX IF NOT EXIST",
    "blocking_mode": "AccessExclusiveLock"
  },
  {
    "locked_item": "user_profiles",
    "blocked_pid": 78300,
    "blocked_query": "SELECT * FROM public.user_profiles ORDER BY email",
    "blocked_mode": "AccessShareLock",
    "blocking_pid": 78291,
    "blocking_query": "\n      -- Initialize system settings table\n      CREATE TABLE IF NOT EXISTS public.system_settings (\n          key VARCHAR(255) PRIMARY KEY,\n          value JSONB NOT NULL\n      );\n      INSERT INTO public.system_settings (key, value) \n      VALUES ('auto_approve', 'false'::jsonb) \n      ON CONFLICT (key) DO NOTHING;\n\n      -- Dynamic Migration: Change intern role to user in constraints and tables\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'user_profiles' AND column_name = 'role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'user_profiles' AND column_name = 'role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.user_profiles DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.user_profiles SET role = 'user' WHERE role = 'intern';\n              ALTER TABLE public.user_profiles ADD CONSTRAINT user_profiles_role_check CHECK (role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'access_requests' AND column_name = 'requested_role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'access_requests' AND column_name = 'requested_role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.access_requests DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.access_requests SET requested_role = 'user' WHERE requested_role = 'intern';\n              ALTER TABLE public.access_requests ADD CONSTRAINT access_requests_requested_role_check CHECK (requested_role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      -- Uploader By tracking field migrations\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n      ALTER TABLE public.motor_test_runs ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n\n      -- Add main_image and gallery_images to public.motors\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS main_image TEXT;\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS gallery_images JSONB DEFAULT '[]'::jsonb;\n\n      -- Initialize escs and propellers tables\n      CREATE TABLE IF NOT EXISTS public.escs (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_esc_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE TABLE IF NOT EXISTS public.propellers (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_prop_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE INDEX IF NOT EXIST",
    "blocking_mode": "AccessExclusiveLock"
  },
  {
    "locked_item": "user_profiles",
    "blocked_pid": 78300,
    "blocked_query": "SELECT * FROM public.user_profiles ORDER BY email",
    "blocked_mode": "AccessShareLock",
    "blocking_pid": 78134,
    "blocking_query": "SELECT id, role FROM user_profiles WHERE email = $1",
    "blocking_mode": "AccessShareLock"
  },
  {
    "locked_item": "motors",
    "blocked_pid": 77958,
    "blocked_query": "SELECT DISTINCT company FROM motors WHERE company IS NOT NULL AND company != '' ORDER BY company",
    "blocked_mode": "AccessShareLock",
    "blocking_pid": 77859,
    "blocking_query": "SELECT max_thrust, recommended_esc, motor_name, custom_parameters FROM motors",
    "blocking_mode": "AccessShareLock"
  },
  {
    "locked_item": "motors",
    "blocked_pid": 77958,
    "blocked_query": "SELECT DISTINCT company FROM motors WHERE company IS NOT NULL AND company != '' ORDER BY company",
    "blocked_mode": "AccessShareLock",
    "blocking_pid": 77918,
    "blocking_query": "\n      -- Initialize system settings table\n      CREATE TABLE IF NOT EXISTS public.system_settings (\n          key VARCHAR(255) PRIMARY KEY,\n          value JSONB NOT NULL\n      );\n      INSERT INTO public.system_settings (key, value) \n      VALUES ('auto_approve', 'false'::jsonb) \n      ON CONFLICT (key) DO NOTHING;\n\n      -- Dynamic Migration: Change intern role to user in constraints and tables\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'user_profiles' AND column_name = 'role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'user_profiles' AND column_name = 'role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.user_profiles DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.user_profiles SET role = 'user' WHERE role = 'intern';\n              ALTER TABLE public.user_profiles ADD CONSTRAINT user_profiles_role_check CHECK (role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      DO $$ \n      DECLARE \n          r RECORD;\n      BEGIN \n          IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name = 'access_requests' AND column_name = 'requested_role') THEN\n              FOR r IN \n                  SELECT constraint_name \n                  FROM information_schema.constraint_column_usage \n                  WHERE table_name = 'access_requests' AND column_name = 'requested_role' \n                    AND constraint_name LIKE '%check%'\n              LOOP\n                  EXECUTE 'ALTER TABLE public.access_requests DROP CONSTRAINT ' || quote_ident(r.constraint_name);\n              END LOOP;\n              UPDATE public.access_requests SET requested_role = 'user' WHERE requested_role = 'intern';\n              ALTER TABLE public.access_requests ADD CONSTRAINT access_requests_requested_role_check CHECK (requested_role IN ('guest', 'user', 'admin'));\n          END IF;\n      END $$;\n\n      -- Uploader By tracking field migrations\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n      ALTER TABLE public.motor_test_runs ADD COLUMN IF NOT EXISTS uploaded_by VARCHAR(255);\n\n      -- Add main_image and gallery_images to public.motors\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS main_image TEXT;\n      ALTER TABLE public.motors ADD COLUMN IF NOT EXISTS gallery_images JSONB DEFAULT '[]'::jsonb;\n\n      -- Initialize escs and propellers tables\n      CREATE TABLE IF NOT EXISTS public.escs (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_esc_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE TABLE IF NOT EXISTS public.propellers (\n          id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n          name VARCHAR(255) NOT NULL,\n          brand VARCHAR(255) NOT NULL,\n          price VARCHAR(100),\n          currency VARCHAR(50),\n          url TEXT,\n          sku VARCHAR(255),\n          main_image TEXT,\n          gallery_images JSONB DEFAULT '[]'::jsonb,\n          custom_parameters JSONB DEFAULT '{}'::jsonb,\n          created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL,\n          CONSTRAINT uq_prop_name_brand UNIQUE (name, brand)\n      );\n\n      CREATE INDEX IF NOT EXIST",
    "blocking_mode": "AccessExclusiveLock"
  },
  {
    "locked_item": "motors",
    "blocked_pid": 77958,
    "blocked_query": "SELECT DISTINCT company FROM motors WHERE company IS NOT NULL AND company != '' ORDER BY company",
    "blocked_mode": "AccessShareLock",
    "blocking_pid": 77960,
    "blocking_query": "SELECT category_id, COUNT(*)::int AS cnt FROM motors GROUP BY category_id",
    "blocking_mode": "AccessShareLock"
  },
  {
    "locked_item": "motors",
    "blocked_pid": 77958,
    "blocked_query": "SELECT DISTINCT company FROM motors WHERE company IS NOT NULL AND company != '' ORDER BY company",
    "blocked_mode": "AccessShareLock",
    "blocking_pid": 77954,
    "blocking_query": "SELECT id, category_id, motor_name, company, max_thrust,\n                         recommended_esc, recommended_propeller,\n                         link_motor, link_esc, link_propeller, custom_parameters, uploaded_by\n                  FROM motors ORDER BY max_thrust ASC LIMIT $1",
    "blocking_mode": "AccessShareLock"
  },
  {
    "locked_item": "motors",
    "blocked_pid": 77958,
    "blocked_query": "SELECT DISTINCT company FROM motors WHERE company IS NOT NULL AND company != '' ORDER BY company",
    "blocked_mode": "AccessShareLock",
    "blocking_pid": 77964,
    "blocking_query": "SELECT DISTINCT company FROM motors WHERE company IS NOT NULL AND company != '' ORDER BY company",
    "blocking_mode": "AccessShareLock"
  },
  {
    "locked_item": "motors",
    "blocked_pid": 77958,
    "blocked_query": "SELECT DISTINCT company FROM motors WHERE company IS NOT NULL AND company != '' ORDER BY company",
    "blocked_mode": "AccessShareLock",
    "blocking_pid": 77959,
    "blocking_query": "SELECT category_id, COUNT(*)::int AS cnt FROM motors GROUP BY category_id",
    "blocking_mode": "AccessShareLock"
  },
  {
    "locked_item": "motors",
    "blocked_pid": 77958,
    "blocked_query": "SELECT DISTINCT company FROM motors WHERE company IS NOT NULL AND company != '' ORDER BY company",
    "blocked_mode": "AccessShareLock",
    "blocking_pid": 77961,
    "blocking_query": "SELECT id, category_id, motor_name, company, max_thrust,\n                         recommended_esc, recommended_propeller,\n                         link_motor, link_esc, link_propeller, custom_parameters, uploaded_by\n                  FROM motors ORDER BY max_thrust ASC LIMIT $1",
    "blocking_mode": "AccessShareLock"
  }
]