# ThrustVault - Python Dependencies
# Used by Render.com (and local dev) to install dependencies.

# Web framework — serves static files and /api/config endpoint
flask==3.1.0

# Production WSGI server — required by Render to run the app
gunicorn==26.0.0

# Direct PostgreSQL database driver (pure Python, compatible with Python 3.15+)
pg8000==1.31.5

# AWS SDK for Python (used for Cognito and RDS IAM DB Auth)
boto3==1.43.33





